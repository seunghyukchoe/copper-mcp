# v0.13 integration checkpoint — 2026-09-05

Continuation: [September 6 balanced-readiness checkpoint](balanced-readiness-checkpoint-2026-09-06.md).
The historical observations below are retained; consult the continuation for later corrections.

## Worktree and authority

Implementation is in `/Users/seunghyukchoe/Desktop/12_PCB-worktrees/v013-integration`, branch
`codex/v013-integration`. It started from the PR #267 foundation and locally integrated #259 at
`1ed83e4`; #265 composed DRC was selectively integrated. The original `12_PCB` main checkout and
its unfinished dirty drafts are preserved. No push, main merge, tag, release or user-board apply
was performed. The package is still 0.12.0.

## Implemented increments

- Four-role CI, three supported interpreters, additive fast/full/compat/evidence commands,
  source-pinned fresh B-141 replay separate from cached report contracts, and provisional
  release-blocking hosted budgets. The protected `Python 3.12` check gates all roles.
- Bounded native optimization jobs, durable redacted metadata with owner/CAS/fencing, placement
  search, private route composition, repeated real KiCad DRC/DRC-backed DFM, optional captured
  Circuit Intent ERC, inconclusive physics domains, and five stdio MCP tools.
- Default-off trusted-host confirmation and separate full-board disclosure. Neither grants apply
  authority. Parent delivery must validate exact bytes and complete response before a human prompt
  is allowed; it is rechecked after consent. Imports are restricted to inventoried Python sources.
- Two-through-eight signal-layer disposable serialization and optional composed route-bundle DRC.
- Authorized Colima/Docker installation and a dedicated no-host-share VM. Both pinned external
  router images passed actual public synthetic execution/format smokes through the restricted
  runner. Production conversion/disposal is not integrated. See the
  [runtime guide](../integrations/local-router-runtime.md) and
  [image/smoke details](../../hardware/optimization-router/README.md).
- Lazy routing facade preserves exported identities/types while avoiding unrelated eager imports
  in the isolated policy worker. Its one-second deadline has not been relaxed.

## Evidence and unresolved failures

The first broad fast run took 2245.84 seconds, with 4435 passes, 13 failures and three skips.
That failed run does not establish any speed target. Subsequent focused runs passed 193 native
foundation/isolation/MCP tests; 48 isolation/MCP/container tests with one unconfigured external
skip; and 35 container/MCP tests with **both real external smokes enabled**. These overlapping
groups are not additive totals. Strict mypy passed all 133 source modules after the import change.

Follow-up validation on 2026-09-06: `make lint`, repository secret scanning, Ruff format, and
wheel/sdist/PCM builds passed. The CI/classifier/budget/mutation-anchor/checkout/documentation
group passed all 64 tests. The final policy/isolation/refusal group had 76 passes and one remaining
one-second policy-worker timeout; it is not green. All implementation changes are staged locally,
not committed or published. Build artifacts remain local development artifacts under `dist/`.

Independent scoped review accepted the final source-identity and parent-publication fixes, with
no remaining P1/P2 findings in that scope. This is not a whole-repository or release verdict.
The separate scoped lazy-facade review also accepted the change: 33 facade/policy/protocol tests
passed independently, representative fresh imports worked, and no import cycle was found. That
does not erase the separately observed intermittent policy timeout or certify all import paths.
CI mutation anchors and checkout metadata were repaired; full mutation re-execution remains
distinct from checking that anchors and killing tests still exist.

The broad failures included obsolete contract expectations/mutation anchors, cold-start timeouts,
and route-aware replay metrics diverging when a wall-clock-limited placement search stops at
different work counts. A read-only diagnosis reproduced the policy one-second timeout and showed
HTTP tool inventory was correct before its 15-second subprocess timeout. Numerous CPU-heavy
background graph processes were observed and left untouched. Startup import reduction helps but
cannot certify hard timing on a contended host. Audio-runner timeout diagnosis needs its exact
failure output; no source mutation was established. Do not solve these by hiding tests or
relaxing production safety budgets.

## Next exact actions

1. Use this worktree's Python 3.12 `.venv`, not the original checkout's editable environment.
   Recheck final focused results and run `make check PYTHON=.venv/bin/python` on a stable tree.
   Run the current-source B-141 evidence command only while no source files are being edited.
2. Finish cold-start/HTTP and deterministic-work replay diagnosis; separate controlled benchmark
   work ceilings from production wall-clock refusal without weakening provenance. Re-measure the
   full fast path against a comparable serial baseline on an otherwise quiet host.
3. Connect reviewed KiCad-to-DSN/SRJ production bridges and mandatory external candidate disposal,
   using the installed local immutable images. Preserve identity/revision/geometry/DRC gates.
4. Complete fresh zone fill, cross-layer multi-pin routing, bounded repair coordination, normal
   workspace schematic ERC and parity, rendering, and optional Orca advisory scheduling/quality
   measurement. OrcaRouter is already incorporated, not pending merge work.
5. Assemble and measure the held-out licensed corpus, execute real host confirmation checks, obtain
   successful hosted main calibration, perform independent integrated review, and finish release
   ledgers. None of these is replaced by checklist or synthetic-test success.

The original [staged release plan](v0.13-supervised-optimization.md) remains incomplete. No 90%
routing, zero post-apply regression, full ordinary-board coverage, full physics judgement, 3x
local speedup, under-20-minute PR path, green `make check`, or v0.13 release is claimed here.
