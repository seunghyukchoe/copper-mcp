# Risk Register

| ID | Risk | Likelihood | Impact | Mitigation | Status / owner |
|---|---|---:|---:|---|---|
| R-001 | Malformed or oversized PCB input exhausts the parser. | Medium | High | Bounded reads, typed validation, fuzzing, no file execution. | Open / security |
| R-002 | Model output bypasses constraints or triggers excessive agency. | High | High | Allowlisted policy actions, deterministic geometry/DRC, separate apply authorization. | Open / architecture |
| R-003 | A candidate is applied to a board changed after routing began. | Medium | High | Content hashes, live revision recheck, atomic undoable commit. | Open / KiCad integration |
| R-004 | Dependency or GitHub Action compromise affects contributors/releases. | Medium | High | Minimal dependencies, Dependabot, audit, CodeQL, immutable action pins, scoped permissions, a non-publishing release dry run, and tag-only attestations/releases. | Mitigated / release |
| R-005 | Benchmark leakage or selected examples exaggerate quality. | High | Medium | Project-family splits, multiple seeds, publish failures, immutable benchmark ledger. | Open / research |
| R-006 | DRC-clean output is treated as electrically or manufacturably safe. | Medium | High | Explicit preview warnings, separate hardware validation records, missing-evidence checklists, physics/DFM gates, prototype measurements, and professional review before fabrication approval. | Open / documentation |
| R-007 | KiCad subprocess or report-schema drift yields unsafe or stale evidence. | Medium | High | Fixed arguments, timeout, bounded private report, fail-closed schema parsing, board revision recheck. | Mitigated / KiCad integration |
| R-008 | Random native identities or volatile exporter metadata obscure meaningful hardware-review diffs. | Medium | Medium | Semantic UUIDv5 identities, read-only temporary replay by default, exact snapshot hashes, and a separately authorized artifact refresh. | Mitigated / Audio Board Lab |
| R-009 | The narrow Board IR v0.1 KiCad subset is mistaken for general KiCad compatibility, causing unsupported geometry or rules to be ignored. | High | High | Fail closed with structured diagnostics, publish an explicit accepted/rejected matrix, keep constraints typed and separate, and expand coverage only with regression fixtures. | Open / Board IR |
| R-010 | Internal A* grid checks are mistaken for authoritative DRC or a general autorouter result, or Board IR obstacle counts amplify search work. | High | High | Candidate-only API; narrow support matrix; typed rejection of unmodeled geometry; explicit internal-metric naming; independent grid, expansion, obstacle-count, and obstacle-check ceilings with cancellation checkpoints; keep KiCad export, DRC, preview, MCP, and apply gates unimplemented until independently verified. | Open / routing |
| R-011 | Disposable candidate bytes are mistaken for applied copper or durable DRC evidence, or a route is serialized against mismatched source context. | Medium | High | Exact source/profile/snapshot binding, candidate hash and deterministic replay checks, bounded deterministic segment identities, full Board IR round trip, no writes or MCP/apply surface, and a separate future candidate-evidence contract. | Open / KiCad integration |
| R-012 | Disposable serialization bypasses caller output budgets, amplifies native-identity collision work, or misattributes the derivative to KiCad. | Medium | High | Revalidate converted content with caller limits; reject base-plus-patch total-object overflow; collect UUID/tstamp identities once; record CopperMCP writer/version provenance; retain bounded Board IR round-trip verification. | Mitigated / KiCad integration |
| R-013 | Rewriting a derivative changes the source revision and silently churns Board IR identities for modeled KiCad geometry without native IDs. | Medium | High | Require a native UUID/tstamp on every modeled source geometry object before serialization; reject revision-derived identities explicitly; preserve exact candidate/base-revision binding. | Mitigated / KiCad integration |
| R-014 | A DRC summary is misbound to a different candidate, source, or rule/library context, or inconsistent/mutable counts alter accepted evidence after validation. | Medium | High | Bind candidate, Board IR, raw source, patched board, patched context, and nested summary revisions; exact-replay serialize from one capture; recheck private and original live contexts; cross-check process status against report findings and type totals against aggregates; freeze copied summary counts; return detached serialization; keep candidate boards private and ephemeral. | Mitigated / KiCad integration |
| R-015 | A public route preview is mistaken for an applied, durable, or generally supported routing capability, or it discloses board geometry to a host that should not receive it. | High | High | Explicit `routed`/`not_routed`/`unsupported_board` status; typed non-echoing diagnostics and bounded conversion-code counts instead of raw adapter text; caller-supplied typed constraints only; workspace-confined bounded reads; wall-clock deadline over the existing integer ceilings; no file, job, export, or apply side effect; documented geometry-disclosure boundary so hosts can decline the tool; opt-in authoritative DRC that fails closed rather than returning unverified evidence. | Open / routing |
| R-016 | Structural Board IR inspection leaks proprietary design content, or per-service request validation drifts apart and opens a gap in one surface. | Medium | High | Counts, digests, units, and standard KiCad layer names only, with a regression test asserting no names, coordinates, UUIDs, or typed identities appear in the serialized document; one shared `request_boundary` module owning field, type, range, boolean, and character rules for every public service; fail-closed conversion reporting diagnostic codes rather than adapter text. | Mitigated / Board IR |
| R-017 | An obstacle model that under-approximates existing copper produces routes KiCad rejects, or a board mixing net classes is routed to the looser clearance. | Medium | High | Exact integer rectangles for axis-aligned pads and orthogonal segments; ceiling half-widths; bounding boxes for round shapes that over-approximate only; obstacle inflation by the stricter of the routed and obstacle net-class clearances; fail-closed rejection of arcs, zones, vias, off-axis rotations, diagonals, and partially routed nets; shared model with the Dijkstra optimality oracle; and a committed blocked-pad fixture whose detour is confirmed by real KiCad 10.0.5 DRC. | Mitigated / routing |
| R-018 | Removing the via rejection is mistaken for multilayer routing support, or a via's clearance is modeled from its drill rather than its copper. | Medium | Medium | Obstacle built from the outer diameter, never the drill; bounding box over-approximates only; via obstacles counted against the obstacle budget; a via on the routed net still fails closed as partial routing; ADR, roadmap, and routing baseline state that this removes one of three blockers, and the routing baseline carries the current per-net measurement rather than a headline number. | Mitigated / routing |
| R-019 | A stale KiCad fill cache or an under-approximated zone outline permits a short, while treating the full outline as physical copper is mistaken for fill-aware routing or real-board progress; high-vertex rings may also amplify work. | Medium | High | Never trust `filled_polygon`; conservatively occupy the complete simple outline; exact integer containment/intersection/rational-distance predicates; strictest routed-class, zone-net-class, and zone clearance; same-net zones rejected as partial routing; each zone counts against the object ceiling and every vertex/bounds/edge relation consumes the obstacle-check budget with cancellation; explicit 0/14 CopperTone re-measurement and separate future refill/freshness contract. | Mitigated / routing |
| R-020 | Public audio-project pages are mistaken for open datasets, causing restricted schematics or layouts to enter fixtures, or a low-level routing check is presented as circuit design, electrical safety, or fabrication evidence. | High | High | Record the two reviewed sites as reference-only and non-redistributable; never fetch them in the runner; accept executable fixtures only from one repository-confined bounded snapshot with exact artifact/licence hashes, SPDX licence evidence, derivation and safety fields, no third-party content, disjoint claims/non-claims, and claims derived from observed results; keep mains/high-voltage cases out of defaults and add schematic/ERC/simulation/hardware gates separately. | Mitigated / research |
| R-021 | Model-authored logical input injects KiCad syntax, exhausts parsing/rendering, creates ambiguous connectivity, collides deterministic identities, or is mistaken for an electrically or board-ready schematic. | High | High | Strict bounded schema and codec; unknown/duplicate/numeric rejection; typed references and complete pin ownership; canonical content digest; escaped strings; collision and deterministic replay tests; original embedded symbols; empty footprints and `on_board=no`; 1 MB pure-render cap; real KiCad parse/netlist verification; no MCP/CLI/file-writing surface; explicit ERC, value, parity, electrical, and fabrication non-claims. | Mitigated / architecture |
| R-022 | Public schematic delivery leaks proprietary topology into ordinary model context, treats a content digest as authorization, crosses clients over stateless HTTP, retains artifacts without bound, or overwrites a workspace file. | High | High | Redacted build record; independent opaque 256-bit capability; non-enumerable stdio-only process store with 15-minute lifetime, 16-entry/16 MiB aggregate and 1 MiB per-artifact ceilings, deterministic LRU, uniform unavailable response, digest recheck, and no persistence/logging; HTTP registration disabled; CLI requires an explicit workspace-confined create-new `.kicad_sch` target with symlink/traversal rejection and no overwrite mode; per-build ERC/electrical/parity/readiness non-claims. | Mitigated / delivery |
| R-023 | KiCad DRC inherits user-global configuration, plugins, library tables, temporary paths, or credential-bearing environment variables, making evidence host-dependent or exposing secrets to the child process. | Medium | High | Build the child environment from a minimal allowlist (`PATH=os.defpath`, `LANG/LC_ALL=C`) plus private per-run HOME, KiCad, XDG, runtime, and temporary roots; inherit no caller entries or KiCad overrides; reject state-tree symlinks and special files; enforce existing per-file, file-count, cumulative-byte, and scan-time ceilings. | Mitigated / KiCad CLI |
| R-024 | A future semantic/visual scene or placement agent is mistaken for authority and writes model-generated geometry directly into KiCad, or the observation leaks more design content than intended. | High | High | Versioned bounded Circuit Scene IR; capability-specific redaction; typed placement intent only; immutable revision-bound previews/candidates; deterministic snapping, connectivity, clearance, rule and provenance validation; explicit separately authorized apply; direct AI KiCad mutation remains prohibited. | Open / architecture |
| R-025 | A 15-minute artifact TTL is described as immediate deletion or secure memory erasure, while expired bytes, allocator pages, returned copies, crash dumps, or swap can persist beyond capability expiry. | Medium | High | Define TTL solely as an absolute access deadline; reads never renew it; lazily reclaim expired store entries on later reads/insertions and process exit; keep store count/byte caps; document that hosts own returned copies and that no secure-erasure guarantee exists. | Mitigated / delivery |
| R-026 | A workspace path is validated and then reopened after replacement, or a case-variant output suffix bypasses the intended create-only artifact boundary. | High | High | Open every input path component relative to one held workspace descriptor with no-follow semantics; use the same final descriptor for type/size validation, bytes, and before/after state checks; anchor output publication and verification to a held directory descriptor; accept only exact lowercase `.kicad_sch`; reject symlinks, parent replacement, existing files, and case variants. | Mitigated / filesystem |
| R-027 | A captured KiCad file table redirects DRC to environment-expanded, absolute, remote, or plugin-backed libraries outside the evidence snapshot, or the child resolves relative state from the live repository/workspace. | High | High | Parse and validate captured file-table URIs before execution; permit only dependencies confined to the private snapshot; reject environment, absolute, remote, and plugin schemes; run the minimal-environment child with private `TMPDIR` as cwd; retain private state/context budgets and post-run revision checks. | Mitigated / KiCad CLI |
| R-028 | MCP wrapper coercion or extra-field smuggling bypasses the intended Circuit Intent contract, or validation errors echo proprietary scalar/list content. | High | High | Close outer wrapper, nested content, and structured output schemas; reject scalar, list, and extra-field shapes at each boundary; return non-echo generic failures; retain redacted output regression checks. | Mitigated / MCP |
| R-029 | KiCad replaces a DRC report with a symlink/FIFO, blocks the reader, emits duplicate/non-finite/deep JSON, or writes unrecognized state elsewhere in the private design snapshot. | High | High | Open the report no-follow and nonblocking; require a regular file from descriptor metadata; enforce duplicate-key, non-finite, nesting-depth, and decoded-value limits; make the private snapshot tree read-only and validate the complete tree for every side effect before accepting evidence. | Mitigated / KiCad CLI |
| R-030 | A caller mutates an artifact object after insertion so the capability serves bytes that no longer match identity or aggregate byte accounting. | High | Medium | Snapshot exact immutable content, digest, and size into each store entry; use only stored size for quotas and eviction; verify stored digest against stored bytes on every read. | Mitigated / delivery |
| R-031 | A release tag is created from a commit without version-specific notes or an explicit record that its full gate authorized tagging. | Medium | High | Require a dated changelog section matching the version and an append-only `Ready` release-authorization row naming the exact clean-`make check` commit; treat authorization, tag creation, artifact build, and publication as distinct states. | Mitigated / release governance |
| R-032 | A silent coordinate-convention error in a source adapter places geometry plausibly but wrongly, so every downstream contract is exact about the wrong board. | Medium | High | The KiCad footprint quarter-turn map is pinned by a committed fixture whose expected pad positions are adjudicated by KiCad's own connectivity engine rather than by arithmetic in this repository; the adapter refuses footprint-local zones, footprint graphics on copper or Edge.Cuts, and non-orthogonal footprint transforms, so no other object class inherits the transform; a CopperTone test asserts no foreign track endpoint lies inside a foreign pad, which is the cross-check that exposed the original defect. | Mitigated / Board IR |
| R-033 | The committed CopperTone board's mounting-hole rule areas were generated as octagons inscribed at the required 2.85 mm keep-out radius, so their edges sit 0.2169 mm inside the requirement and copper may legally approach closer than the constraint intends. | Low | Medium | The generator now circumscribes the octagon so edges hold the full radius; a dedicated test records that the committed board still carries the older inscribed keepout; regeneration is deferred to its own slice because new coordinates invalidate every measurement recorded against the board's current revision digest, and that slice must update the recording test and re-measure coverage in the same change. | Open / hardware |
| R-034 | Board IR 0.2's rectangular front-courtyard support is mistaken for general footprint fidelity, or exposed courtyard geometry is mistaken for a completed placement-legality check. | High | High | Fail closed on back-side footprints, non-orthogonal transforms, mismatched layers, non-rectangular/open/filled/mixed courtyard primitives, and more than 64 rings; bind pose, ownership, side, lock, and canonical board-frame rings into the snapshot digest; charge relationships and vertices to request budgets; keep `courtyard_overlap` fixed at `not_modelled`; refuse locked moves; require source-oracle fixtures and a new contract review before expanding the subset or enabling placement apply. | Open / Board IR |
| R-035 | A Board IR layered candidate may be mistaken for KiCad-valid copper, or conservative lattice envelopes may under-model a physical edge/via/clearance rule. | High | High | Keep the adapter proposal-only and out of MCP, serializer, DRC, fill, and apply paths; verify snapshots and revisions; include via diameter in edge bounds; use separate via-radius envelopes and safe rotated-pad bounds; reject unsupported selected-net copper, arcs, constraints, and outlines; record B-018 with deterministic replay, no-path, stale, off-grid, source-immutability, and tamper evidence; require source-preserving round-trip serialization plus authoritative KiCad DRC before promotion. | Open / routing |
| R-036 | A non-orthogonal Board IR pad angle could be mistaken for an unsupported parent footprint pose, or a valid layered obstacle budget exhaustion could be collapsed into malformed input and trigger retry loops. | Medium | Medium | Keep parent poses restricted to four orthogonal turns, render pad rotations with exact decimal tokens, reparse the disposable placement candidate, and return a distinct `obstacle_budget_exceeded` diagnostic with a regression test. | Mitigated / placement and routing |
| R-037 | A layered candidate could be rendered with missing vias, reversed layer spans, stale geometry, or an identity collision and then be mistaken for KiCad-valid copper. | High | High | Require the original request replay, verify candidate identity and native identities, restrict to two signal layers/full-stack through-vias, canonicalize stack order, bound output/object counts, and require a Board IR round trip. Keep DRC, MCP, and apply authority deferred. | Mitigated / serializer; DRC open |
| R-038 | A layered candidate could pass the internal lattice and serializer checks while KiCad rejects its clearance, via, or connectivity semantics, or the DRC evidence could describe a changed private context. | High | High | Require the original request replay before rendering; run the derivative through the existing fixed-argument private KiCad DRC snapshot; bind candidate, Board IR, source, patched board, and complete context revisions; accept only strict aggregate evidence with process/report agreement; recapture the live context and refuse on any change. Keep the boundary internal and read-only. | Mitigated / layered candidate DRC; public routing remains open |
| R-039 | A stale or orphaned `filled_polygon` island could be treated as exact copper and open a corridor that KiCad no longer pours, or a large island set could bypass routing budgets. | High | High | Accept fill only from the explicit freshness-bound authority; require source revision and matching Board IR zone/net/layer for every island; replace a conservative zone only when matching evidence exists; inflate exact polygon obstacles with the strictest clearance; charge islands to object and edge budgets; refuse malformed provenance and keep public fill-aware provenance unadvertised until its response contract is reviewed. | Mitigated / internal fill-aware core; public provenance open |
| R-040 | An AI host could receive a fresh-fill record but misunderstand whether it shaped routing or only proved connectivity. | Medium | High | Keep fill opt-in; return only aggregate source/context/fill digests; require a closed `routing_effect` literal (`foreign_zone_obstacles`, `connectivity_evidence`, `both`, or `verified_context`); bind evidence to the board revision; never return raw islands or mutation authority; cover the public schema and source-preservation path in B-022. | Mitigated / routing |
| R-041 | A malformed layered request or an under-inflated foreign obstacle could escape the bounded diagnostic path or produce a route whose copper envelope violates clearance; a high-clearance zone could also be lost when exact fill replaces its outline. | Medium | High | Validate obstacle entries and all search settings before budget classification or geometry work; gate stale diagnostics on an explicit expected revision; inflate foreign track/via/zone envelopes by candidate half-width/via radius; associate fill with the strictest zone clearance for its net/layer; cover each boundary with focused regressions and B-023. | Mitigated / routing |
| R-042 | A public layered candidate may be mistaken for KiCad-valid, directly applicable copper, or a complete multilayer route; overly large pad references, geometry, or diagnostics could also disclose private board details or exhaust a host. | High | High | Keep `preview_layered_route` candidate-only and read-only; require double CAS and infer nets from pad refs; bound settings and response geometry; verify canonical candidate IDs; use closed status-specific schemas and non-echoing diagnostics; state explicitly that public DRC, serializer/export, durable jobs, multilayer generalization, and apply remain open; cover deterministic replay and negative CAS cases in B-024. | Open / public layered routing |
| R-043 | A persisted routing job could leak board data, be replayed against a newer revision, be guessed as a bearer handle, or be overwritten by a stale worker; TTL/capacity mistakes could also exhaust the local service. | High | High | Persist only closed redacted job metadata and content-addressed digests; bind completion to candidate identity and source/snapshot CAS; use bounded canonical records, SQLite transactional updates qualified by `job_id` and revision, deterministic idempotent creation, cooperative cancellation, restart/expiry tests, and one error for unknown/expired IDs. Keep worker leases, per-principal authorization, candidate geometry/export, ordinary MCP tools, and Tasks extension negotiation open. | Mitigated / internal ledger; execution open |
| R-044 | A topology heuristic could be mistaken for a Steiner-optimal or DRC-safe tree, or its cubic comparisons could exhaust a caller's routing budget on a high-pin net. | Medium | High | Keep the one-Steiner guide bounded to at most nine evolving components; charge every geometric comparison to the existing obstacle-check and cancellation budgets; use stable deterministic tie-breaks; retain `component-mst-v1` above the cap; record policy identity and benchmark limitations; keep KiCad DRC and fabrication validation separate. | Mitigated / routing; higher-degree decomposition open |
| R-045 | Public layered DRC evidence could be mistaken for whole-board or general multilayer acceptance, or a live/durable caller could persist an option that the worker ignores. | High | High | Expose evidence only through explicit file-backed `include_drc`; bind candidate/base/source/patched/context digests; share the bounded deadline and fail closed on unavailable authority; force `Literal[False]` in live/job schemas and reject direct job preparation; return aggregate findings only; retain narrow-fixture and no-fabrication/FreeRouting claims. | Mitigated / public layered routing; broader authority open |

> **Amendment — 2026-08-03:** ADR-0008 and R-014 supersede R-011's “separate future
> candidate-evidence contract” clause. The internal evidence boundary now exists; durable export,
> preview, MCP/CLI exposure, persistence, and apply remain unimplemented.

> **Amendment — 2026-08-03:** ADR-0009 and R-015 supersede the “keep preview and MCP exposure
> unimplemented” clauses in R-010 and R-014. A bounded, non-mutating preview with opt-in aggregate
> DRC evidence now exists; durable jobs, persistence, export, and apply remain unimplemented.

> **Amendment — 2026-08-03:** ADR-0015 and R-022 supersede R-021's “no MCP/CLI/file-writing
> surface” mitigation. The bounded public service now exists; its only durable effect is one
> explicitly requested new schematic file, while MCP delivery remains ephemeral and stdio-only.

> **Amendment — 2026-08-05:** ADR-0040 and R-040 supersede R-039's final clause. The public
> `preview_route` response now advertises freshness-bound fill provenance when explicitly requested;
> stale/orphan/source-mismatched evidence remains fail-closed and the disclosure is aggregate only.

> **Amendment — 2026-08-03:** R-025 clarifies R-022's “15-minute lifetime” wording: capability
> access expires at the deadline, while stale in-process bytes are reclaimed lazily and secure
> memory erasure is not claimed. R-026 and R-027 extend the filesystem and KiCad-child mitigations
> after the independent audit; historical rows remain unchanged.

> **Amendment — 2026-08-03:** R-031's exact clean-`make check` commit is the validated source
> commit. The later tag commit may differ only in `CHANGELOG.md` and the release ledger, preventing
> the authorization record from becoming a self-referential SHA while keeping code and build inputs
> identical.

> **Amendment — 2026-08-03 (append-only correction):** R-004's row was edited in place by PR #17
> rather than superseded by a new dated entry. As first written it read *"Minimal dependencies,
> Dependabot, audit, CodeQL, attestations, future SHA pinning."* with status *"Open / release"*;
> PR #17 rewrote the mitigation and moved the status to *"Mitigated / release"*. The current row
> is factually right and is left standing; this note restores what the in-place edit removed.

> **Amendment — 2026-08-05:** R-037's “DRC open” qualifier is superseded by R-038 and B-020:
> the layered serializer is now covered by an internal replay-bound authoritative KiCad DRC gate.
> MCP exposure, durable export, multilayer generalization, negotiated congestion, and apply remain
> open, so the layered proposal is not promoted to a general routing capability.

> **Amendment — 2026-08-05:** ADR-0050 and R-045 supersede R-042's “public DRC” qualifier for
> the narrow file-backed preview. Candidate-bound aggregate DRC evidence is now opt-in and
> fail-closed; live preview, durable jobs, refill, multilayer generalization, export, and apply
> remain open.

| R-046 | 2026-08-05 | A conservative spatial index could omit an obstacle and make exact routing unsound. | High | Medium | Store canonical source ordinals, over-approximate closed bounds, sort bucket results, retain a bounded linear fallback, and differentially replay 256 adversarial queries plus indexed/linear A*/Dijkstra routes. Exact predicates remain the authority and all ceilings fail closed. | Mitigated / routing; adversarial and cross-board scaling coverage remain open |
| R-047 | 2026-08-05 | An unsigned attestation payload could be mistaken for authenticated provenance, or a permissive predicate projection could disclose private board details. | High | High | Use fixed redacted names, required SHA-256 bindings, a closed MCP schema, aggregate DRC byproducts only, deterministic canonical serialization, and focused tamper/redaction tests. State prominently that signing, verification, persistence, and remote transport remain open. | Mitigated / attestation payload; DSSE and verifier design remain open |
| R-048 | 2026-08-05 | Placement DRC could validate a disposable board against a changed source/rule/library context or silently accept unsupported footprint syntax. | High | Medium | Admit only the replay-verified native-ID subset; capture and digest every permitted context file; run fixed private KiCad DRC with no refill/save; validate the private tree and re-capture the original context; fail closed before KiCad on unsupported geometry; keep aggregate findings only. | Mitigated / placement DRC evidence; broader geometry and live/apply CAS remain open |
| R-049 | 2026-08-05 | Review-bot boundary gaps could leak raw net names or executor diagnostics, misbind a completed candidate to a different job policy, let expired manifests reappear after rollback, or construct unbounded layered envelopes before refusal. | High | High | Restrict public refs to content-derived IDs; map failures to fixed typed messages; bind candidate completion and manifests to exact request identity and measured work; commit TTL purges before unavailable responses; validate budgets before hashing and stop obstacle construction at the ceiling; retain candidate-only/read-only contracts and focused regression evidence. | Mitigated / routing and live observation; hard pre-emption and general routing authority remain open |
| R-050 | 2026-08-05 | Negotiated routing could reduce one conflict while being mistaken for exact PCB clearance, or an adversarial net count/iteration budget could multiply A* work beyond a host's limits. | High | High | Restrict the first slice to bounded two-pin requests on one common lattice; count exact unit edge/vertex resources; cap nets, iterations, total expansions, obstacle checks, and penalty history; reroute only immutable candidates; bind policy configuration into candidate identity; return structural overflow and fixed diagnostics; keep exact pairwise clearance, KiCad DRC, multilayer capacity, and FreeRouting comparison open. | Mitigated / internal routing slice; physical and broad-corpus validation remain open |
| R-051 | 2026-08-05 | Expired routing metadata could survive an unavailable read because a transaction rolls back the purge after the row is found to be expired. | Medium | High | Commit the bounded TTL purge before returning the uniform not-found/unavailable error for job and candidate stores; retain rollback for malformed rows and all other failures; assert durable deletion at the SQLite boundary. | Mitigated / retention boundary; broader multi-tenant lifecycle remains open |
| R-052 | 2026-08-05 | A worker could retain an in-memory lease after its expired durable job disappears during cancellation or publish-race cleanup, blocking future work indefinitely. | Medium | High | Clear `_active` in `finally` around every post-claim durable lookup/transition; preserve unavailable/conflict errors rather than retrying; replay expiration with a fake clock and assert a subsequent claim succeeds. | Mitigated / worker lifecycle; distributed lease coordination remains open |
| R-053 | 2026-08-05 | A spatial-index benchmark could count only final hits while claiming exact-relation work reduction, overstating the benefit of the acceleration. | Medium | Medium | Instrument candidate ordinals before the exact bounds predicate, compare ordered identities for correctness, regenerate the artifact from the implementation commit, and label historical metrics as superseded. | Mitigated / benchmark evidence; cross-board scaling remains open |
| R-054 | 2026-08-05 | Live KiCad capture could spend its entire route budget in serialized-board counting or misclassify a deadline expiry during confirmation, while a blocking official IPC call remains uninterruptible. | High | High | Pass the cooperative deadline callback into bounded S-expression traversal; preserve typed deadline errors; state the synchronous-wrapper limitation explicitly; keep isolated worker/process pre-emption as an open gate. | Mitigated / cooperative timing; hard pre-emption and hostile-session isolation remain open |
| R-055 | 2026-08-05 | A KiCad `kicadxml` export could be mistaken for proof of source intent, or hostile XML could exhaust parsing or disclose component/net text. | High | High | Require exact canonical schematic replay before parsing; accept only the bounded format-E passive subset; reject DTD/entities/processing instructions/unknown structures and duplicate connectivity; cap bytes, elements, depth, and text; return fixed error classes and digest/count evidence only. Keep ERC, schematic-to-PCB parity, and electrical validation separate. | Mitigated / schematic parity; broader KiCad netlist formats and authoritative ERC remain open |
| R-056 | 2026-08-05 | A back-side footprint could be mirrored twice or a narrow rectangular courtyard observation could be mistaken for general KiCad placement fidelity. | High | High | Record side explicitly, import authored board-frame child coordinates without a second mirror, require matching `F.CrtYd`/`B.CrtYd`, refuse inner-layer/non-orthogonal/non-rectangular forms, and pin an asymmetric front/back source plus headless DRC fixture. Keep GUI flip-save, side-aware legality, serializer, and apply separate. | Mitigated / Board IR observation; GUI and general topology evidence remain open |
| R-057 | 2026-08-05 | A placement candidate could pass despite overlapping rectangular courtyards, or a cross-side overlap could be incorrectly rejected by a board-frame-only predicate. | High | High | Transform each saved Board IR ring into the proposed pose with exact integer quarter-turns; compare only same-side rectangular rings; treat edge contact as non-overlap; make `violated` part of candidate legality; reject unsupported topology before evaluation; replay same-side, cross-side, and absent-courtyard cases. Keep nonzero clearance and general topology open. | Mitigated / placement legality; configurable rules and broader KiCad geometry remain open |
| R-058 | 2026-08-05 | Placement apply could mutate the wrong revision, accept a route capability, or publish an unsupported pose; a concurrent KiCad write could be overwritten. | High | High | Bind the operation domain into the HMAC payload; require explicit token plus operator opt-in; verify candidate/source/snapshot CAS before and under an exclusive lock; refuse KiCad lockfiles; replay the immutable candidate through the narrow serializer; create an exact backup and use atomic replacement with truthful post-rename status. Keep side flips, general topology, DRC, live IPC, undo, and remote authority open. | Mitigated / bounded placement apply; broader geometry and post-action verification remain open |
| R-059 | 2026-08-05 | Placement post-publication verification could accept a concurrent rewrite, report the stale published digest after rollback, or leave a capability reusable after bytes were renamed. | High | High | Re-render the authorized candidate against the exact source, reparse the bytes read after publication, guard rollback by the published digest, spend the token on every post-rename/post-publication path, and re-read the final on-disk revision for the response. Keep KiCad DRC, undo, live mutation, and unsupported geometry open. | Mitigated / bounded publication boundary; broader post-action/editor coverage remains open |
| R-060 | 2026-08-05 | A synthetic audio microcase could be mistaken for a production audio-board corpus or for evidence that routing is electrically, physically, or manufacturably correct. | High | High | Keep the fixture independently authored with exact Apache-2.0/hash metadata; run only bounded candidate preview and disposable serialization; assert source preservation and deterministic identities; publish explicit non-claims for apply, KiCad DRC, electrical behavior, production, fabrication, and hardware measurement. | Mitigated / benchmark scope; real audio-board and hardware gates remain open |
| R-061 | 2026-08-05 | Placement apply could return `applied` after a visible concurrent rewrite that landed after post-publication verification, or clients could mistake `applied_but_unverified` for a guaranteed new revision. | High | High | Take a final best-effort digest observation immediately before success; spend the capability and return `applied_but_unverified` when the observed digest differs; document that the final digest may be the restored original or concurrent bytes and retain the residual last-nanosecond transaction race as an explicit open risk. | Mitigated / bounded publication boundary; longer editor transaction and post-action KiCad observation remain open |
| R-062 | 2026-08-05 | Apply could return the published digest as `applied` after a final observation failed because the board was deleted or became unreadable. | High | High | Use a non-fallback final observation at the success boundary; consume the capability and return `applied_but_unverified` with `board_revision_after=null` plus a fixed diagnostic when no digest can be read. Keep the fallback only for post-rename recovery reporting. | Mitigated / bounded publication boundary; longer editor transaction and post-action KiCad observation remain open |
| R-063 | 2026-08-05 | Public placement DRC evidence could be mistaken for legalizer proof, expose raw board findings, or be bound to a different source/context; warning-only reports could be advertised as clean. | High | High | Keep DRC behind explicit file-backed `include_drc`, replay only the immutable supported serializer candidate in a disposable private context, bind candidate/source/patched-board/context revisions, return aggregate counts only, separate `passed` from `clean`, reject live/apply flags, and fail closed on timeout, malformed, stale, foreign, or context-raced authority. | Mitigated / bounded evidence disclosure; broader geometry, live editor CAS, apply post-action DRC, electrical/fabrication signoff, and FreeRouting comparison remain open |
| R-064 | 2026-08-05 | A post-placement scene and DRC could describe different revisions, or a caller could mistake a claimed candidate id for mutation provenance. | High | Medium | Require expected digest; capture board/rule/library context once; derive both artifacts from it; re-capture context after DRC and discard on change; return aggregate DRC only; accept no candidate/token fields. A post-final-observation filesystem race and live-editor CAS remain open. | Mitigated / file-backed observation |
| R-065 | 2026-08-05 | Padless courtyard collision is covered only for supported rectangular Board IR v0.2 geometry; custom clearance and arbitrary courtyard topology remain open. | Medium | Medium | Keep padless footprints out of subject/anchor/rule resolution, retain their immutable rectangular courtyards as stationary same-side envelopes, and replay baseline/collision outcomes in B-050. | Mitigated / bounded placement legality; custom clearance and general topology remain open |
| R-066 | 2026-08-05 | A live KiCad snapshot could spend its operation budget in UTF-8/S-expression parsing before a cooperative deadline check, while a callback might be mistaken for hard pre-emption. | High | High | Pass the deadline callback into parsing, check before/after decode and at 4 KiB checkpoints, preserve typed deadline errors, and measure the expired-large-payload refusal in B-051. Keep process isolation as the hard-preemption gate. | Mitigated / cooperative parser timing; atomic decode and blocking IPC remain open |
| R-067 | 2026-08-05 | Malformed or stale post-placement requests could spend DRC context budgets before refusal, or a padless footprint could be hidden behind an infeasible-rule diagnostic. | High | Medium | Parse the complete scene request before filesystem work; compare the expected digest to the first board capture before sidecar scans; require context bytes to match; reject padless rule references before contradiction checks; replay malformed/stale/order regressions and a derived workspace-state benchmark. | Mitigated / bounded pre-work and placement boundary; live/editor CAS and general geometry remain open |
| R-068 | 2026-08-05 | A cancellation callback could raise during negotiated routing and leave a partial iteration or ambiguous failure status. | High | Medium | Treat callback exceptions as typed cancellation at each A* checkpoint; stop the negotiated iteration; publish no partial candidates; replay callback failure at a nonzero iteration. | Mitigated / cooperative candidate boundary; hard pre-emption and physical routing authority remain open |
| R-069 | 2026-08-05 | A layered candidate could be re-stamped with dimensions that differ from the Board IR net-class constraints while retaining a valid topology digest. | High | High | Resolve the routed net's assignment and require exact track/via width/diameter/drill equality before topology acceptance; add altered-dimension replay. | Mitigated / structural layered verification; padstack, DRC, fabrication, live apply, and FreeRouting comparison remain open |
| R-070 | 2026-08-05 | A stale cancellation request or malformed lookup could strand a non-terminal routing job or leave expired redacted metadata durable. | High | High | CAS-acknowledge `CANCEL_REQUESTED` after the lease boundary; purge expired rows before identifier validation and commit the purge on unavailable/malformed lookup; replay worker recovery and durable SQLite deletion. | Mitigated / local lifecycle boundary; distributed leases, MCP Tasks, and broader retention policy remain open |
| R-071 | 2026-08-05 | A post-placement benchmark could report zero workspace mutations after an observer touched or replaced a file with identical bytes because content-only snapshots omit metadata changes. | Medium | Medium | Include stable inode, mode, and nanosecond mtime metadata for files and symlinks in the before/after digest; add a metadata-only touch regression and record the clean-tree B-056 replay under a new artifact path. | Mitigated / disposable benchmark evidence; filesystem transaction provenance and live-editor observation remain open |
| R-072 | 2026-08-05 | Live route capture could consume the IPC adapter's default timeout before the route budget is charged, allowing a slow snapshot to outlive the proposal's advertised deadline. | High | High | Create the route deadline before capture; pass an absolute deadline and a 1–10,000 ms remaining timeout; replay propagation with a one-second budget. Keep hard pre-emption and blocking IPC as open limits. | Mitigated / cooperative live-route timing; process isolation and live editor authority remain open |
| R-073 | 2026-08-05 | Live placement capture could consume the IPC adapter's default timeout before the placement budget is charged, allowing a slow snapshot to outlive the proposal's advertised deadline. | High | High | Create the placement deadline before capture; pass an absolute deadline and a remaining timeout capped at the adapter's two-second bound; replay propagation with a one-second budget. Keep hard pre-emption and live editor authority as open limits. | Mitigated / cooperative live-placement timing; process isolation and live editor authority remain open |
| R-074 | 2026-08-05 | A negotiated-routing cancellation at the next iteration boundary could return a partial candidate from an earlier unsuccessful pass, making incomplete work appear usable. | High | Medium | Clear candidates, connections, overflow, and wire-length evidence on any cancellation boundary; return all ordered nets unrouted; replay a candidate-plus-no-path first pass followed by next-iteration cancellation. | Mitigated / cooperative candidate boundary; hard pre-emption and physical routing authority remain open |
| R-075 | 2026-08-05 | Count-based eviction of consumed apply nonces could make a still-valid capability replayable after the operator restores its pre-apply backup. | Critical | High | Retain all consumed nonces until individual expiry; sweep only expired entries; reject malformed capacity hints; cover a tiny-cap replay-pressure regression; document pre-issuance admission control as the hard-memory-bound extension. | Mitigated / process-local replay boundary; memory admission, remote key custody, and multi-tenant authorization remain open |
| R-076 | 2026-08-05 | Live placement could spend most of its configured budget capturing and converting a board, then silently receive a second full legalization/DRC window because the shared file-backed helper started a fresh deadline. | High | High | Create the deadline before IPC, pass the same absolute deadline through `_preview_placement_source`, compute remaining seconds immediately before legalization, refuse with `budget_exhausted` when expired, and replay capture-plus-work timing with a one-second budget. Keep cooperative blocking-IPC limits and hard process pre-emption open. | Mitigated / cooperative live-placement timing; process isolation, live editor authority, and broader placement geometry remain open |
| R-077 | 2026-08-05 | Pressure to reach an OpenSSF Criticality target could incentivize artificial commits, releases, issues, comments, or contributor claims, degrading project integrity and obscuring real security or adoption work. | High | High | Treat the score as a diagnostic; require material changes for releases, preserve append-only dated inputs, report official versus reconstructed values separately, require two repeated snapshots plus engineering gates for the `0.4` claim, and exclude private or synthetic activity. | Open / project governance |
| R-078 | 2026-08-05 | A generic negotiated backend could return self-hashed but illegal or non-reference-equivalent geometry, or the verification replay could double-spend caller-authorized search budgets. | High | High | Bind every result to the clipped request; independently replay non-reference backends with the deterministic A* core; allocate each call half of the remaining expansion/obstacle budget; reject mismatch atomically; verify pairwise candidate clearance before publication. | Mitigated / bounded one-layer coordinator; generic linear verifier acceleration, board-wide geometry, and KiCad DRC remain open |
| R-079 | 2026-08-05 | A placement heuristic could mistake a Manhattan proxy for routed quality, consume a second deadline window during legalization, or retain a candidate that bypasses existing placement legality. | High | Medium | Retain only legalizer-issued immutable candidates; rank a labeled proxy, not route length; cap search/rank/legalizer work; propagate remaining deadline and cancellation to every score/legalizer call; make callback exceptions fail closed. | Mitigated / advisory solver; optimality, routing, DRC, KiCad mutation, and physical signoff remain open |
| R-080 | 2026-08-05 | An AI policy trace or selected option could expose board-sensitive identifiers/geometry, be dictionary-tested from predictable content, or be misconstrued as authorization to create copper. | High | High | Use closed bounded decoding, canonical input/decision digest checks, supplied-option membership, ordinal-only action tokens, redacted traces, and deterministic validation downstream. Explicitly document digest linkability and retain policy outside route integration. | Mitigated / advisory privacy boundary; complete-record dictionary testing and any learned-policy quality claim remain open |
| R-081 | 2026-08-05 | Advertising MCP Tasks without durable owner-bound lookup could turn a task ID into an unauthorized bearer result handle or lose a task after restart. | Critical | High | Do not advertise or implement Tasks wire methods; retain only random owner-bound process-local handles with uniform failure and bounded retention; require authenticated owner/session binding and durable reviewed storage before adoption. | Mitigated / deferred protocol boundary; durable cross-restart Tasks lifecycle remains open |
| R-082 | 2026-08-05 | A FreeRouting comparison could leak private paths/tokens, treat arbitrary external tool output or self-attested receipts as proof, or claim a routing result without equivalent KiCad DRC. | High | High | Use private work directories, minimal child environment, bounded files/output, redacted diagnostics, common-source/result hashes, and KiCad DRC prerequisites; force self-attested receipts to `unavailable_or_incomplete`. | Mitigated / diagnostic harness; constrained KiCad import, constrained candidate runner, sandboxing, and a real licensed comparison remain open |
| R-083 | 2026-08-05 | The documentation could overstate a nonexistent generic linear candidate verifier, call copper-edge spacing centreline clearance, treat linkable policy digests as secret redactions, or imply that bounded external-process execution provides sandbox containment. | Medium | High | Preserve the original rows as audit history; append the D-108/B-062 correction; name deterministic A* replay as the current custom-backend gate; label the 300,000 nm metric as copper-edge clearance; disclose complete-record digest dictionary risk; and state that FreeRouting receipt/process controls cannot complete a comparison or sandbox user-supplied code. | Mitigated / documentation and evidence correction; generic independent verifier, real comparison controls, and policy trust-boundary review remain open |
| R-084 | 2026-08-05 | An expired private routing export could remain durable because the deliberately uniform unavailable exception rolls back its TTL purge, or a cooperative cancellation arriving after placement search work could expose a ranking indistinguishable from a completed bounded result. | High | High | Commit the export-store purge before raising the uniform unavailable response; regression-inspect the SQLite row after expiry. Recheck cancellation at solver publication and return an empty ranked set when observed; replay cancellation after legalization and during successor scoring. State the limits: TTL is not secure erasure and cooperative callbacks do not hard-stop work already in progress. | Mitigated / local retention and advisory-publication boundary; durable-media erasure, caller-held copies, hard pre-emption, routing/DRC, and KiCad mutation remain open |
| R-085 | 2026-08-05 | A request-store unavailable or wrong-context lookup, or an unauthorized lookup of a live export, could roll back unrelated expiry cleanup and retain expired private payloads; conversely, a broad cleanup change could remove or expose the unauthorized live target. | High | High | Commit prior TTL cleanup before the intentionally uniform unavailable response. Regression-test expired request deletion and an unauthorized live export lookup that removes a distinct expired export while leaving the live row present and protected. Preserve rollback for decode/integrity failures and state that TTL does not securely erase durable media or caller-held copies. | Mitigated / local SQLite retention boundary; secure erasure, backups, returned copies, durable multi-user authorization, routing, and apply remain open |
| R-086 | 2026-08-05 | A malformed routing request/export handle could receive the uniform unavailable response before a transaction starts, leaving unrelated expired private records durable until another valid lookup occurs. | High | High | Validate the timestamp before a transaction, then begin/purge before handle validation; commit any expiry cleanup before returning the uniform unavailable response. Regression-test malformed request/export handles against expired rows and inspect SQLite after refusal. State that TTL does not securely erase durable media, backups, or caller-held copies. | Mitigated / local malformed-handle retention boundary; invalid-time behavior, secure erasure, durable multi-user authorization, routing, and apply remain open |
| R-087 | 2026-08-05 | Public routing service prevalidation or malformed candidate-manifest lookup could bypass the retention-owning store, leaving expired private records durable or yielding diagnostics that reveal more than a uniform miss/not-found. | High | High | Let request/export stores validate their own handles after timestamp-first purge, and purge manifest records before malformed-ID refusal. Regression-test public malformed handles and manifest IDs against expired rows, retaining fixed diagnostics and no disclosure. State that TTL does not securely erase durable media, backups, or caller-held copies. | Mitigated / local service and manifest retention boundary; secure erasure, durable multi-user authorization, routing, export, and apply remain open |
| R-088 | 2026-08-05 | MCP entrypoint validation of malformed get/cancel/export handles could reject before purge-first service/store validation, retaining expired private records or prompting a broad schema relaxation that leaks data or alters valid lifecycle behavior. | High | High | Admit only the handle forms needed to reach downstream validation; keep MCP payloads closed, fixed diagnostics/no disclosure, timestamp-first retention handling, and valid-path regressions. Test malformed get/cancel/export calls with expired records and inspect persisted cleanup. State that TTL does not securely erase durable media, backups, or caller-held copies. | Mitigated / local MCP retention boundary; secure erasure, durable multi-user authorization, routing, export, and apply remain open |
| R-089 | 2026-08-05 | A caller-selected, malformed, or failing policy could silently fall back to unproven routing, alter retry authority or geometry, exhaust work, or make a profile-enabled candidate indistinguishable from a no-profile v2 candidate; an in-process policy could also outlive cooperative cancellation. | High | High | Keep the registry private and immutable; derive one bounded coordinator-owned input only after request validation; evaluate once; require exact input/decision bindings, a complete known-net permutation, and no unsupplied selections; fail closed with zero router calls/candidates/connections; retain coordinator retries and budgets; and use a separate v3 composite identity/evidence binding. Preserve the no-profile v2 result shape and identity byte-for-byte. | Mitigated / internal initial-order slice; hard pre-emption, isolated remote/plugin/model adapters, learned-policy quality, KiCad DRC, manufacturing, FreeRouting parity, and apply remain open |
| R-090 | 2026-08-05 | An ambient-environment dependency audit can report vulnerabilities from unrelated globally installed packages and obscure the declared project graph, while a project resolution can still be mistaken for reproducible or installed-runtime evidence. | Medium | High | Pass `.` to `python -m pip_audit` in Make and CI, retain the secret scan, and regression-test the exact project-path commands. Treat results as time-bound resolver evidence: indexes/network and advisory data are mutable; no lockfile or hash pinning establishes reproducibility; the optional `kicad` extra requires explicit installation/resolution; and runtime drift needs a separate environment audit. | Mitigated / dependency-audit scope; lock/hash reproducibility, advisory latency, optional-extra coverage, and installed-runtime verification remain open |
| R-091 | 2026-08-05 | A project-path audit can be mistaken for coverage of optional dependency groups because the CI environment installs `.[security]`, leaving `dev`, `security`, or `kicad` vulnerabilities unreported. | Medium | High | Define `python -m pip_audit .` as the default production dependency-graph audit and record that project-path mode excludes all optional groups (`dev`, `security`, and `kicad`) without an extras selector. Pin the CI secret-scan and audit steps by exact regression; require separate explicit optional-group audits or lock evidence, and retain separate installed-runtime auditing. | Mitigated / dependency-audit scope; optional-group audit targets/locks, lock/hash reproducibility, advisory latency, resolver/index/network mutability, and installed-runtime verification remain open |
| R-092 | 2026-08-05 | A valid route candidate could be committed as a completed lifecycle record after export/manifest storage rejects its bounded geometry, or a request expiry and malformed cancellation reason could retain redacted lifecycle metadata. | High | High | Persist candidate artifacts before completion; worker persistence failure becomes terminal and non-completed, while direct publication refuses before completion and remains revision-bound retryable; preserve only TTL-bounded unreadable orphans on a completion race. On request lookup failure, purge lifecycle metadata at the same captured timestamp; resolve the repository retention boundary before rejecting optional cancellation text. Regression-inspect SQLite rows and completed-state absence. | Mitigated / local durable routing lifecycle; SQLite secure erasure, backups, caller-held exports, distributed transactions, hard pre-emption, multi-user authorization, and apply remain open |
| R-093 | 2026-08-05 | Artifact-first publication could store geometry for a candidate that the later lifecycle CAS rejects on kind, router, policy, seed, base, endpoint, identity, or work-ceiling binding, consuming capacity or creating unnecessary private retention. | High | High | Extract the exact pure completion validator and run it before every export/manifest write; run it again inside `RoutingJobRecord.complete` at the CAS. Direct publication preserves retryability on refusal; worker execution terminalizes with fixed `invalid_request`; regressions cover kind/router/policy/seed/metrics mismatches and inspect both artifact tables. | Mitigated / local preflight and completion boundary; TTL-bounded orphan artifacts remain possible only for valid candidates that lose a later completion/cancellation race; secure erasure, distributed atomicity, remote authorization, DRC, and apply remain open |
| R-094 | 2026-08-05 | Artifact-first publication could store a valid candidate while the lifecycle is queued, terminal, cancellation-pending, or at an unexpected revision; final completion would then fail and retain an unnecessary private orphan. | High | High | Reuse a non-mutating completion-eligibility preflight requiring `RUNNING` plus the exact expected revision before every artifact write, and repeat it in the final CAS. Regress queued/completed/failed/cancelled/cancel-requested/wrong-revision direct publication attempts and assert unchanged lifecycle plus empty artifact tables. A state change after preflight remains a race, not an authorization claim, and may retain only a TTL-bounded inaccessible artifact for an otherwise valid candidate. | Mitigated / local lifecycle/artifact boundary; secure erasure, distributed atomicity, remote authorization, DRC, and apply remain open |
| R-095 | 2026-08-05 | An isolated policy child could return a semantically valid but differently serialized receipt, inherit process context, outlive cancellation, or be mistaken for a model/OS-sandbox boundary, allowing ambiguous provenance or overclaimed isolation. | High | High | Reconstruct and byte-compare every response against its canonical encoding; bind request/response with a 256-bit nonce and SHA-256 digest; reject duplicate/oversized/noncanonical frames; launch only the fixed `sys.executable -I` reference worker with replacement environment, closed FDs, discarded stderr, temporary cwd, POSIX session, deadline/cancellation kill, and best-effort CPU/file-size ceilings; state the macOS sandbox non-claim. Regression-test raw subprocess bytes, hostile frames, timeout/cancellation, and process arguments. | Mitigated / internal deterministic reference-only protocol; filesystem/network/device sandboxing, non-reference/model/plugin workers, remote execution, routing integration, KiCad DRC, manufacturing, and apply remain open |
| R-096 | 2026-08-05 | A local IPC diagnostic could crash before reporting an expected credential absence, collapse token/endpoint/settings failures into misleading server state, or exceed its stated observation window during conversion; a successful fake run could also be mistaken for a live-editor result. | High | High | Check only whether both KiCad plugin variables are present before resolving settings; return a canonical skip otherwise; emit fixed redacted configuration classes; share one monotonic deadline through capture and conversion checkpoints; prove no mutation and no post-expiry conversion in fake-client tests; and record workstation configuration separately from runtime output. | Mitigated / local read-only diagnostic; cooperative timing, real plugin-run evidence, blocking-call isolation, DRC, routing, placement, apply, and fabrication remain open |
| R-097 | 2026-08-05 | A hostile board or model-directed tool chain could try to convert an inspection, route preview, or valid-looking token into unauthorized source access, mutation, or output/report disclosure. | High | High | Exercise the public route and placement apply handlers with mutation enabled and syntactically valid unauthorized tokens; require structured `invalid_token` before source access. Keep schemas closed, revisions bound, annotation output quarantined, quotas explicit, and same-run workspace snapshots metadata-aware without serializing dynamic metadata. | Mitigated / local MCP output/report and capability boundary; host logs, provider telemetry, remote authorization, unknown attacks, electrical/DRC/fabrication safety, and live-editor behavior remain open |
| R-098 | 2026-08-05 | A frozen local-repair result can still be forged or mutated by hostile in-process code, causing a diagonal, repeated, out-of-window, blocked, wrong-endpoint, wrong-bend, or digest-unbound local path to be mistaken for a deterministic repair. Calling a window “coordinator-owned” could also overstate unauthenticated provenance. | High | High | Revalidate the request and verify every completed result against it before a future integration: exact input/route digests, endpoints, unique in-window unblocked unit-orthogonal cells, bend count, expansion ceiling, and fixed terminal diagnostics. Name repair windows conventionally coordinator-supplied only; add no ownership/authentication claim. Keep the type standalone until route-candidate binding, reference replay, physical gates, and held-out evidence exist. | Mitigated / standalone local-proposal boundary; authenticated provenance, Board IR/KiCad binding, DRC, physical clearance, negotiated integration, model quality, and apply remain open |
| R-099 | 2026-08-05 | A caller-recomputable construction digest could be misread as request authentication or immutable object sealing, while rejecting a fully valid current request after an in-process field change would provide neither property nor useful boundary semantics. | High | High | Remove construction-digest comparison. Revalidate and canonical-copy only the current exact bounded fields before callbacks/search; malformed/type-confused values return `invalid_request` with zero search work and no callback. Treat a valid current-value change as a new valid submission, while the strict result verifier continues to reject forged or invalid geometry against that current canonical request. Document no origin, ownership, authentication, or sealing claim. | Mitigated / standalone current-value validation boundary; authenticated provenance, Board IR/KiCad binding, DRC, physical clearance, negotiated integration, model quality, and apply remain open |
| R-100 | 2026-08-05 | Integrating the subprocess profile could let a stale, malformed, identity-swapped, or noncanonical worker response influence routing, silently fall back to unprofiled routing, or be mistaken for safe model execution or OS sandboxing. | High | High | Admit one private fixed profile and executable only; retain the nonce/digest-bound canonical protocol and process limits; recheck the exact reference policy ID, input digest, complete unique known-net order, empty selections, decision digest, and composite identity before router construction; return fixed refusal or cancellation with zero router calls and no fallback. Record the literal profile in policy evidence and keep retry, geometry, physical, and work-budget authority in the coordinator. | Mitigated / fixed deterministic initial-order worker only; filesystem/network/device sandboxing, authentication/signature, non-reference model/plugin workers, routing-quality improvement, KiCad DRC, manufacturing, and apply remain open |
| R-101 | 2026-08-05 | Equal result counts on one trivial FreeRouting smoke fixture, or the recorded unequal process timings, could be presented as parity, quality, performance, or whole-board evidence. Separate hashes and self-attested source/report, source/DSN-export, import, and runner receipts could also be mistaken for causal workflow proof, while bounded execution could be mistaken for a sandbox. | High | High | Pin the exact official JAR URL/SHA and original fixture; retain strict KiCad 10.0.5 report parsing, result hashes, source-preservation checks, minimal child environment, and bounded process/file/output limits. Label the CopperMCP path as a standalone pure-kernel/non-MCP runner and force incomplete self-attested evidence to `comparison_closed=false` and `unavailable_or_incomplete`. Require harness-owned constrained import/candidate transactions, an equivalent timing protocol, and a broader corpus before comparison claims. | Mitigated / real two-pad smoke observation only; causal workflow attestation, parity, performance, routing quality, broader boards, MCP/apply-service coverage, executable sandboxing, and comparison closure remain open |
| R-102 | 2026-08-05 | A public unkeyed digest of `KICAD_API_TOKEN` can act as an offline oracle for guesses of the limited-input credential; accepting that legacy `sha256:` wire type would preserve the disclosure risk and permit a stale compatibility downgrade. | Medium | High | Generate a fresh process-local 256-bit key; compute only domain-separated HMAC-SHA256 session revisions; require the explicit `hmac-sha256:<64 hex>` schema; use constant-time comparison after format validation; reject legacy and malformed values before IPC/conversion; never serialize token/key/tag outside the existing request echo. | Mitigated / same-process live-session CAS; process-memory compromise, remote authentication, token issuance, KiCad ABA, synchronous IPC pre-emption, DRC, and mutation authority remain open |
| R-103 | 2026-08-05 | A placement request combining a known padless external proposal anchor with an unrelated syntactic contradiction could be reported as `infeasible_constraints`, masking the earlier unsupported-geometry boundary and making the failure taxonomy depend on incidental rule composition. | Medium | Medium | Scan rule references and then every non-`None` proposal anchor before contradiction analysis; reuse the exact known-padless `unsupported_geometry` refusal; publish no candidate; regress all five supported anchor points with conflicting front/back side rules; retain pure-contradiction and genuinely absent-reference behavior. | Mitigated / known-padless validation ordering only; anchor geometry, padless placement, non-padless placement behavior, general constraint solving, KiCad DRC, apply, and mutation are not expanded |
| R-104 | 2026-08-05 | A process-local keyed HMAC over `KICAD_API_TOKEN` can still be classified by CodeQL as a non-expensive hash of limited-input sensitive data; retaining its wire type would leave the hosted alert unresolved and could make the prior mitigation appear sufficient for brute-force resistance. | Medium | High | Supersede only the derivation with fixed 200,000-iteration PBKDF2-HMAC-SHA256 using a fresh 256-bit process-local domain-separated salt and 32-byte output; require `pbkdf2-hmac-sha256:<64 hex>`; preserve constant-time CAS, strict pre-conversion rejection, and no token/salt output. Record fixed work and a broad runtime guard; retain the prior HMAC record as history. | Mitigated / same-process live-session CAS and hosted-query-aligned construction; process-memory compromise, remote authentication, token issuance, KiCad ABA, synchronous IPC pre-emption, DRC, and mutation authority remain open |
| R-105 | 2026-08-05 | A public live-route flow could be unusable when the only session CAS is private, encouraging a client to reconstruct or disclose `KICAD_API_TOKEN`; separately, an explicitly declared padless subject could be masked by unrelated syntactic infeasibility. | Medium | High | Return the bounded opaque PBKDF2 session revision (or explicit null without a token) in the closed observation schema and require the existing fixed-format non-null CAS at preview; regression-compose only public observation/scene values and prove token/salt non-disclosure and token/process stale refusal. Preflight subject refs in request order before existing rule/anchor scans and contradiction analysis; regress the fixed no-candidate diagnostic. | Mitigated / local candidate-only composition and padless taxonomy; process-memory compromise, remote authentication, KiCad ABA, hard IPC pre-emption, general placement solving, DRC, apply, and mutation remain open |
| R-106 | 2026-08-05 | A locally derived or hostile in-process candidate path could be mistaken for Board-IR-safe copper despite stale binding, forged identity, oversized work, foreign-net attachment, repeated/off-grid geometry, or a skipped obstacle edge; separately, documenting this internal replay as coordinator integration, DRC, or FreeRouting parity could overstate its authority. | High | High | Require exact current-value reconstruction and candidate identity verification; bind snapshot/request/candidate revision, net, layer, width, endpoints, settings, seed, single-path ordering, and zero vias; replay every decompressed orthogonal unit edge against the reference router's exact integer Board-IR predicate under capped path-edge/obstacle budgets and cooperative stop checks. Permit only legal same-net attachment nodes; return typed redacted failures. Keep the validator internal and non-publishing: no coordinator integration, public API/MCP, candidate-pair replacement, DRC, FreeRouting parity, or apply. Retain declined integration until the predeclared >=10% gain gate is met. | Mitigated / internal exact candidate-path acceptance subset; general Board IR geometry, multilayer/vias, complete coordinator transaction and accounting, KiCad DRC, FreeRouting comparison, routing-quality proof, and board mutation remain open |
| R-107 | 2026-08-05 | A status-only workspace preflight, caller-selected directory, or fallback result path could allow Java/KiCad probes or DRC to launch on a caller board outside a quota/private boundary; a resulting record could be mistaken for sandboxed or comparable evidence. | High | High | Require an internal provider-created aggregate-quota capability; reject noncanonical/symlinked/foreign/shared/undersized roots; create every child below it; run all harness probes there; copy and immediately rehash every caller-supplied result before live DRC; and fail closed with zero subprocesses when no capability exists. | Mitigated / disabled provider seam; reviewed OS-specific quota plus runtime-access policy, candidate-runner transaction, broader corpus, parity, performance, and comparison closure remain open |
| R-108 | 2026-08-05 | A related-fixture replay with relaxed caps could be labelled as the pinned predeclared exact-local-repair experiment, overstating the provenance and meaning of a negative admission result. | High | Medium | Retain B-071 as immutable audit history, append its correction, independently reconstruct the original semantic fixture, pin its source commit and snapshot digest, and regress the helper Board IR, requests, digest, and settings against the original builder. | Mitigated / benchmark-attribution correction only; coordinator integration, routing-quality improvement, DRC, FreeRouting parity, and board mutation remain open |
| R-109 | 2026-08-05 | A partially connected, branching, diagonal, curved, filled, self-intersecting, or hostile high-vertex courtyard graphic could be mistaken for a reliable physical envelope; a bounding-box-only comparison could also falsely report concave cavities as collisions, while unbudgeted polygon work could exhaust a placement request. | High | High | Accept only matching-side unfilled rectangles/polygons and complete non-branching orthogonal line cycles; preserve exact integer rings, retain generic self-intersection validation, and use doubled-integer strip/interval positive-area overlap with a charge at every strip, edge, and interval comparison. Reject unknown topology before view construction and retain the operation deadline/check ceiling. Pin a KiCad-resaved, DRC-clean polygon/line-chain fixture plus malformed-chain and overlap/clear regressions. | Mitigated / simple orthogonal observation and candidate legality only; arcs/curves/diagonals, nonzero clearance, arbitrary KiCad courtyard semantics, side flips, DRC of moved candidates, placement apply, electrical/fabrication signoff, and GUI/live-editor authority remain open |
| R-110 | 2026-08-05 | An original audio topology could be mistaken for a tested product, or independent route candidates could be combined implicitly and presented as a clean whole-board result; untrusted tool output could also be mistaken for a candidate-binding proof. | Medium | High | Bind fixture and Apache licence bytes to SHA-256; state sources/nonclaims; route each declared net from the same immutable source; equality-check the public service against the shared result; serialize and DRC only one candidate at a time in a private workspace; require source preservation and report non-clean counts. | Mitigated / fixture and independent-derivative evidence only; electrical/ERC, candidate composition, board-wide DRC, manufacturing, hardware, FreeRouting comparison, and production authority remain open |
| R-111 | 2026-08-05 | A multi-net preview could leak an implicit mutation/export capability, publish a partial allocation, or treat a replay/clearance result as whole-board signoff. | High | High | Accept only 2–8 distinct revision-bound scene net references; publish only a plan after a complete identical composition replay, zero lattice overflow, and exact bounded cross-net clearance. Keep combined serialization internal/private, issue no token, and record aggregate KiCad DRC only for the public fixture derivative. | Mitigated / narrow two-pin single-layer composition only; no apply/export/persistence, multilayer/via/zone capacity, electrical/fabrication, or general-board claim. |
| R-112 | 2026-08-06 | A schema-valid multi-net request could crash the service with an untyped error instead of a typed refusal, an expired wall-clock budget could be published as a false non-determinism finding against the deterministic core, and two bundles composed under different coordinator policy could share one identity. | Medium | High | Reserve the derived-seed headroom at the request boundary and in the published schema; classify cancellation before replay equality and give budget exhaustion its own diagnostic; bind the coordinator policy-envelope digest into `bundle_id` and publish it. Pin each with a regression that fails when the fix is reverted. | Mitigated / request-boundary, diagnostic, and identity correction only; the two-pin single-layer composition subset, absent apply/export/persistence authority, and open multilayer, electrical, and fabrication questions are unchanged |
| R-113 | 2026-08-06 | A change to canonicalization could silently move a published content address — candidate, bundle, snapshot, manifest, export, job, render, statement, or context digest — through a fully green suite, because almost every identity test only recomputed the digest from the same object it was asserting. Every persisted ADR-0043 job, ADR-0047 manifest, and ADR-0048 export holding the old address would then fail verification at replay rather than at review, and a payload whose bytes change without changing length (an in-flight two-layer via-span reordering did exactly this) is invisible to a length check alone. | High | High | Commit a golden identity for every content-addressed surface in `tests/test_golden_identities.py`: an exact digest recomputed from a committed fixture, plus the canonical payload length wherever a same-length content change is representable. State in the module that changing a pinned value is a breaking change requiring a deliberate version bump and a migration note for persisted artifacts, and index the surfaces already pinned elsewhere so the set stays auditable. | Mitigated / repository-wide identity pinning only; no production behavior changed. Pins prove reproducibility of the current canonicalization, not correctness of the geometry, DRC, electrical, or fabrication claims behind it, and a surface added without a pin remains undetected until this index is revisited |
| R-114 | A live apply's compare-and-swap can be defeated between the check and the write, and a user undo, a concurrent API client, or a commit orphaned by a client crash cannot be detected at all. | Medium | High | Not mitigable at the protocol level and deliberately not presented as such. KiCad's IPC API exposes no revision, dirty flag, or conditional write, so re-reading immediately before staging narrows the window without closing it; `create_items`/`update_items` discard the per-item status the wire protocol returns, so success is re-observed rather than inferred; the commit is held for the shortest possible interval with no intervening round trip, and dropped in a `finally`. A fourth hazard joins the three above and is equally undetectable: an editor that restarted and reopened byte-identical content presents a different session revision, so the capability is refused — but a *different* editor process holding byte-identical content is indistinguishable from the intended one by content alone, and only the session leg separates them. Recorded in [ADR-0074](../adr/0074-live-ipc-one-undo-commit-apply.md) and [the IPC apply research](../research/ipc-apply-v1.md) rather than mitigated. Currently unreachable: the mutation is not implemented. | Open / KiCad integration |
| R-115 | Adopting KiCad's 5,000 nm courtyard cache inset means a placement with under 10,000 nm of real courtyard interference is no longer refused, so a genuine physical overlap can be previewed. | Medium | Medium | Not resolved by refusing, and deliberately not hidden. KiCad — the authority under ADR-0004 — reports that band clear, so refusing it would assert a violation the tool denies; asserting `proven_clear` would deny an overlap the geometry has. The band is reported as `inconclusive` instead, which is a first-class value in the published per-rule evidence, survives into the MCP contract and the apply path, and is never rewritten to `proven_clear`. A caller needing the stricter reading can treat `inconclusive` as a failure; the three-valued vocabulary is what makes that expressible. The band is two orders of magnitude below the IPC-7351 courtyard excess, so no IPC-conformant footprint relies on it. Measured at 0 false-positive violations and 0 false-negative clears over 15 real-KiCad cases (B-089). | Open / placement |
| R-116 | A validation rule can encode an invariant that is simply false about the outside world and still pass every test, when the fixtures are all drawn from the one case where the false rule and the true rule agree. The copper-stack rule required layer ID `= position * 2`; KiCad numbers copper `F.Cu=0, B.Cu=2, In{N}.Cu=2+2N`. The two rules agree on exactly one board shape — two copper layers — which was every fixture in the repository, so 1,900 tests stayed green while every real 4-layer board was refused with `unsupported.construct` (#104). The refusal was fail-closed and never produced a wrong answer, which is why it looked like a capability limit rather than a defect, and it survived from the adapter's first commit. | Medium | High | Rules about an external format are now derived from that format's own specification or implementation, cited by URL in a `docs/research/` note, rather than inferred from the fixtures on hand — and the note must state what it refuses to claim, including version boundaries. The specific class of blind spot is fixture monoculture, so the countermeasure is a fixture whose shape *distinguishes* the candidate rules: this fix adds three-, four- and six-layer conversions plus twelve refusal cases, two of which are the previously-accepted wrong numbering and KiCad's genuine pre-V9 numbering. Residual: the adapter accepts one board format version, so a widened `_SUPPORTED_KICAD_PCB_VERSIONS` crossing the KiCad 9 boundary silently reintroduces a second, incompatible numbering; the constant carries an inline warning and points at the research note, but nothing mechanically enforces it. | Open / KiCad integration |
| R-117 | CopperMCP chains an `Edge.Cuts` outline with a zero tolerance while KiCad chains its own with a non-zero `aChainingEpsilon`, so a board whose outline KiCad accepts and draws, and whose DRC is clean, can still be refused here for endpoints that miss by a fraction of a micrometre. The user sees a tool that disagrees with the editor about whether their board has an outline, and the natural reading is that CopperMCP is broken. | Medium | Medium | Accepted deliberately rather than mitigated, because the alternative is the one direction of error an outline may not take: closing a gap adds board area no drawn segment encloses, and the router would be handed room the fabricated board does not have. The refusal is typed and names the malformation (`geometry.invalid`, "must be one closed non-branching loop") so the fix is actionable in KiCad, where snapping the endpoints is the same fix KiCad's own "not a closed shape" DRC error asks for and where the correction is real rather than assumed. The gap is not silently narrowed by a tolerance constant: there is no tolerance constant to widen. Residual and unmeasured: no survey exists of how often real boards carry sub-tolerance gaps, so the frequency of this refusal in the wild is unknown, and the mitigation is a report from a user rather than a metric this project collects. | Open / KiCad integration |
| R-118 | Rounding a roundrect corner radius up is safe only because no obstacle model in the repository consults the radius — every one of them over-approximates a pad by its full bounding box. That is a property of the current code, not a general truth, and nothing in the type system enforces it. An obstacle model that started using the corner radius to trim the envelope would silently begin under-approximating copper, which is the direction that lets a router place a track through real metal. The same reasoning failure already happened once inside this change: `_pad_cores` inferred "round pad" from a collapsed central band, and a stadium roundrect collapses identically, so it was handed an inscribed square twice as tall as its real copper. | Low | High | The claim is pinned by a test rather than left as prose: `test_rounding_the_radius_never_shrinks_the_pad_obstacle` asserts the obstacle envelope is identical for a near-zero and a one-half ratio and equals the full pad rectangle, so an obstacle model that began reading the radius fails there, and the control test asserts the modelled attachment core lies inside the exact fractional-radius pad by `Fraction` arithmetic. The direction guard was mutation-checked: flipping the ceiling to a floor fails the control on its containment assertion. The disc-core defect is fixed by gating the inscribed square on the pad being a disc, with a stadium case added to the core-containment parametrisation that the prior fixture set — every case of which had a band with real height — could not have caught. Residual: the rounding is recorded on `ConversionResult` but is not surfaced through any MCP tool response, so an agent caller cannot yet read it. | Open / KiCad integration |
| R-119 | A Board IR object whose KiCad UUID the file reuses now carries a revision-derived identity, which is stable for one file revision and changes the moment a single byte of the board changes. Any caller that stored such an ID — a saved placement candidate, a queued routing job, an external note — will not resolve it against the next revision, and the failure presents as an unknown object rather than as an explanation. | Medium | Medium | Accepted, and the alternative is worse: a stable-looking `footprint:kicad:<uuid>` shared by 45 components resolves to the *wrong* component rather than to none, which is the failure this change exists to prevent. The existing seam already forces the safe reading — every source-preserving patch path refuses a snapshot containing any `:derived:` identity, so a reused-UUID board cannot be written back at all and no stored ID can be acted on against a stale revision. Residual and unmeasured: nothing surfaces *why* an object's identity is derived, so an agent caller sees a write-back refusal without learning that the board reuses UUIDs and could be repaired in KiCad; the conversion result carries no counterpart to `max_roundrect_rounding_nm` for identity reuse. Also unmeasured is whether any KiCad version resolves duplicate UUIDs on load, which would make the fallback unreachable for boards round-tripped through the editor. | Open / KiCad integration |
| R-120 | Netless stitching copper makes the connectivity under-claim permanent and invisible: a ground net that is physically stitched through net-0 vias will never be claimed connected, because the claim cannot be net-bound, and a caller who trusts the "unconnected" answer may propose redundant copper or misread a finished board as unrouted. Symmetrically, netless copper is cleared at the widest class on the board, so routes near stitching fields may detour further than the copper's true net would require, and on dense boards that over-inflation can turn a routable request into `no_path`. | Medium | Medium | The asymmetry is the documented design (ADR-0078): obstacles over-approximate and connectivity under-approximates, so both failure modes refuse or under-claim rather than fabricate. The under-claim is pinned by tests — a netless via whose annulus both same-net stubs touch still yields no already-connected claim, and the guard is mutation-checked so a future edit that lets `None` match a request net fails four tests. Residual and unmitigated: no tool response distinguishes "unconnected" from "unconnected, but netless copper is present that KiCad's fill-time connectivity might join", so the caller cannot see that the answer is conservative rather than final. | Open / routing |
| R-121 | 2026-08-06 | Chamfered- and circle-courtyard footprints are now previewable but not movable: the placement apply path (ADR-0065) still refuses to rewrite any courtyard topology beyond unfilled `fp_rect`, so a caller can legalize a candidate this slice previews and then have the apply step refuse it. A second, quieter asymmetry: overlaps confined to a chamfer corner triangle, a circle bounding-box corner, or the circle polygonisation band are `inconclusive`, so a caller that treats only `violated` as failure can preview a placement KiCad would flag once the penetration leaves the conceded band. | Medium | Medium | The apply refusal is typed and fail-closed, never a silent rewrite, and the preview/apply asymmetry is stated in ADR-0080 rather than narrowed; extending the serializer is its own slice. The conceded bands are first-class `inconclusive` values in the published evidence, never rewritten to `proven_clear`, and B-093 measures them at 0 contradictions against the real tool; a caller needing the stricter reading treats `inconclusive` as failure, which the three-valued vocabulary makes expressible. | Open / placement |
| R-122 | An aperture pad is omitted from Board IR *silently*. Diagnostics mean refusal in this project -- every caller treats a non-empty diagnostics tuple as a rejected board -- so there is no channel through which the adapter can say "this document contained objects the IR does not represent", and a caller comparing a KiCad pad count against a Board IR pad count will find them unequal with nothing in the result explaining why. The omission is also justified by an argument about the *pad*, that it carries no copper, and not by any check that the paste opening it defines actually lies over copper the IR does model; a footprint whose copper pad was deleted and whose apertures were left behind converts with the apertures quietly gone. | Low | Medium | Bounded rather than removed. The drop is conditional on five properties that together mean the object can carry neither an obstacle nor an attachment -- no copper layer, paste or mask layers only, `smd` kind, no net, no pad number -- and any pad with no copper failing any one of them is a typed `unknown.layer` refusal, so the silent path is narrow and closed. `test_a_paste_aperture_pad_is_not_copper_and_contributes_nothing` pins it as an *equality* of every copper-bearing field against the same board without the aperture, so a future widening that began dropping something load-bearing fails there rather than passing quietly; the guard was mutation-checked by making it accept every no-copper pad, which fails all four control cases. Residual and deliberate: no count of omitted objects is surfaced to a caller, and the pattern to fix it properly is the one D-157 established for the roundrect residue -- a measured field on `ConversionResult` rather than a diagnostic -- which this change does not implement. | Open / KiCad integration |
| R-123 | An incrementally maintained structure can drift from what a rebuild would produce, and the drift is invisible until a query silently misses an entry — at which point the router treats an occupied resource as free. The negotiated coordinator now keeps ledger occupancy and a spatial index alive across passes instead of rebuilding them, so this failure mode is newly reachable. | Medium | High | Structural first: the index's cell size is fixed at construction, so an entry's cells are a pure function of its bounds and cannot be perturbed by another insert — "mutate in place" and "rebuild from survivors" are the same computation. Contractual second: a query guarantees a **superset**, so both bounded-work fallbacks widen the answer and neither can narrow it. Evidential third: randomized differential tests assert index ⊇ brute force across four cell sizes and three object-size regimes; a 200-mutation sequence must match a fresh build in keys, entry count, bucket count, oversize count and every query answer; and five ledger retention fractions must leave byte-identical added nets, conflict scores, overflow resources and 144 probed penalties. The mutation check in B-094 targets the touching-rectangle guard specifically. Residual: none of this covers the router's per-request obstacle index, which is still rebuilt per `propose()` call and is deliberately out of scope. | Open / `@seunghyukchoe`; ADR-0081, B-094 |
| R-127 | A source-to-board parity `passed` is a claim about *the Circuit Intent's connectivity*, not about the schematic file CopperMCP delivered — and the two artifacts are one flag apart, so the distinction is easy for a caller to lose. The delivered schematic is board-excluded by construction and cannot participate in a parity check at all; what KiCad compared the board against is the board-eligible projection, whose digest is a different value. A caller that reads `passed` as "the schematic I was handed matches this board" has read something stronger than was measured. The narrower version of the same risk: the liveness invariant is an arithmetic property of a footprint-less two-pin passive projection, and it is what stands between an empty parity array and a false pass. A future intent that carries footprint assignments would satisfy the invariant differently, or not at all. | Medium | High | The response contract makes the distinction structural rather than documentary: `parity_projection` is a separate closed object carrying its own `artifact_digest` and a fixed `differs_from_schematic_by: "board_eligibility"` literal, the projection is never returned to a caller as a deliverable, and the service refuses outright if the two digests are equal or if either is unbound from the rendered bytes — each refusal mutation-checked. `verification` states `footprint_correctness`, `electrical_validation` and `erc` as `not_run` and `board_ready` as a `False` literal in the same object, so the non-claims travel with the claim. The invariant is a hard gate, not a heuristic: it is computed from KiCad's own report, a disagreement raises rather than reconciles, and a real KiCad control feeds the board-excluded delivered schematic in and asserts a refusal rather than a pass. Residual and deliberate: nothing prevents a caller from *reporting* the delivered schematic's digest alongside a parity verdict that was computed over the projection, because both digests are in the response by design — the mitigation is that they are distinguishable, not that they cannot be conflated. The invariant's scope is stated in ADR-0084 rather than enforced against a future footprint-bearing intent, which does not exist yet; when one does, it invalidates the invariant loudly (the sum stops matching) rather than quietly. | Open / KiCad integration |
| R-131 | The `contour:assembled:` identity (ADR-0087) is durable against every edit that leaves the outline's member set alone, but not against a tool that rewrites `gr_line` uuids wholesale on save or a user redrawing the board edge: either moves the contour id, and a caller holding the old reference re-reads instead of re-binding. The subtler half: the composite's resolvability argument is scoped to the root `Edge.Cuts` `gr_line` population, so a future outline source (`gr_poly`, arcs) or a KiCad version that changes uuid handling for graphics would need the derivation re-reviewed rather than silently extended. | Low | Medium | The id is content-bound by design, so movement on a real outline change is correct behaviour, and the scene reports the reference as `native` precisely because it survives *unrelated* edits -- the durability a caller is told about is the durability measured. Degradation is fail-closed: any member set the derivation cannot resolve becomes `:derived:` and both apply gates refuse, mutation-checked. The derivation is confined to `_assembled_contour_identity` with the member-population rule stated in ADR-0087, so an outline-source extension fails its review at one function. Survey evidence: 48 of 48 real outline members carry one distinct uuid today, so the degradation path is currently fixture-only. | Open / adapters |
| R-132 | A whole-board scene on a dense board now returns **no segments at all** rather than an arbitrary prefix of them, and a caller that reads only the arrays will see a board whose copper appears unrouted. That is the intended direction of error — the prefix was unusable for any clearance, congestion or connectivity question and was labelled as the whole set — but it is a real capability loss on exactly the boards that motivated the scene. The narrower residual: which kinds survive now depends on the board's contents, so adding objects can change which kinds are withheld between two revisions of one board. | Medium | Medium | The withheld kind is structurally unmissable rather than documented: it is not an array, so a consumer that iterates it raises, one that tests it for emptiness is told it is non-empty, and one that compares it to `[]` finds them unequal — each of those mutation-checked against a fixture that reproduces #127's shape. `objects_omitted` on the withheld kind names exactly how many objects the caller is not seeing, and the bounded-region path that returns them is unchanged and measured unchanged on 11 of 11 real boards. The whole-board request still returns the outline, which is what a caller needs in order to choose that region, so the recovery path is reachable from the truncated response itself. Determinism is per board revision and request, which is what the replay contract requires and all it ever promised; it is asserted across two runs on a truncated dense fixture. Deliberately not mitigated: `max_scene_objects` is unchanged at 2,000, so nothing here reduces how often truncation happens — only what it says when it does. | Open / MCP surface |
| R-133 | Region scoping trades a class of routes for a lower ceiling, and the trade is invisible unless the refusal says so. A route whose only legal detour is wider than `region_margin_nm` is now refused where the whole-board model would have searched for it, and a caller that reads that refusal as "no route exists on this board" has read something stronger than was measured. The sharper form of the same risk is the unsound half of the change: scoping the obstacle model while leaving the lattice spanning the board would let the router propose copper through copper the request never modelled — the one direction of error an over-approximating obstacle model exists to prevent, and one that a candidate, unlike a refusal, carries downstream into apply. | Medium | High | The confinement is structural rather than documentary: every lattice index is derived from the region, so a node the search can expand is a node inside it, and the drop test reuses the exact envelope the spatial index is built with, one lattice step beyond the obstacle's own inflation. Both mutations are covered and were checked by mutating the source: unclipping the lattice fails a fixture whose only way around a wall runs through dropped copper, and removing the one-step term fails a proximity fixture built in exactly that band. Every emitted candidate in the region-scope suite is verified against a whole-board obstacle set recomputed independently from Board IR, not against the set the router modelled, and the region width is swept so a narrow region may refuse but may never emit a path a wider one would call illegal. The capability cost is named by its own code, `no_path_in_region`, whose message carries the configured margin; `docs/agents.md` states that it is not a claim about the board. Residual and deliberate: 10 mm is calibrated against one 11-board mixer corpus, and a board whose nets need longer detours will meet this refusal rather than route. On the external SimpleRouteJson corpus the cost measured as zero — identical routed count, wire length and bend count, with 9 of 11 `no_path` refusals reclassified — but that is two corpora, not a general result. | Open / routing |
| R-134 | An unlocked KiCad group is now accepted and *not modelled*, so a caller can move one member of a group the designer meant to hold together and nothing in the converted board says the grouping existed. Board IR has no membership relation, and `apply_placement_candidate` will splice a single grouped footprint. The failure is silent by construction: the board converts, the move is geometrically valid, and only the designer reopening the file sees a group pulled apart. The sharper form of this risk -- a group whose `(locked yes)` KiCad propagates to every member, so that a member converted at `locked=False` would pass three authorization gates that KiCad itself would refuse -- was found in adversarial review of the first version of this change and is closed rather than carried: such a group is now a typed refusal. | Low | Medium | Bounded and recorded rather than removed. `ConversionResult.unmodelled_group_count` reports how many groups were accepted and not modelled, so a caller that cares can read it and decline -- the pattern D-157 established for the roundrect residue, chosen over a diagnostic because every caller of `parse_kicad_bytes` treats a non-empty diagnostics tuple as a refusal and a warning would refuse the board this change exists to admit. The read side is proven inert by equality: the surveyed backup converted with and without its groups differs only in `source`. The write side is proven byte-preserving rather than assumed -- `test_a_placement_splice_leaves_a_root_group_byte_identical` asserts the group's bytes and the whole document tail are identical after a real placement splice. The lock path is closed from two directions: `test_a_locked_root_group_is_refused_because_lock_reaches_its_members` pins the refusal, and `test_a_locked_group_never_converts_a_member_footprint_as_unlocked` pins the property over the member's own `locked` flag -- the value every gate consults -- so a future change that learns to propagate lock must make it hold rather than delete it. Residual and deliberate: the count is a number, not a membership relation; nothing tells a caller *which* objects were grouped, and no surface refuses a move that splits an unlocked group. A locked group refuses the whole document rather than converting read-only, which is a usability cost accepted in exchange for the gate. Also unmodelled: a group inside a footprint, which stays a typed refusal because no surveyed board carries one. | Open / KiCad integration |
| R-135 | Accepting pad `zone_connect` `1`, `2` and `3` (ADR-0091) is sound only while pad-to-pour connectivity is derived exclusively from verified fill geometry. That property holds today and is enforced by no type: a future surface that infers attachment from a same-net pad sitting inside a zone outline would be reading an attachment mode Board IR discarded, and the three accepted values would become unsound in the same direction the refusal of `0` exists to protect. The narrower residual: the argument for `1`, `2` and `3` is that losing an *attaching* override only under-states `Zone.pad_connection`, which depends on that field continuing to mean what `connect_pads` means. | Low | High | The dependency is named at the point of change, in the docstring of `_require_attaching_pad_zone_connection`, which is the single function any widening or narrowing must edit, and in ADR-0091's consequences. The refusal of `0` is pinned by a test asserting its exact diagnostic, and the acceptance of `1`, `2` and `3` is pinned by a whole-content equality against the same board without the field -- so a future change that starts modelling the value breaks that equality and cannot land silently. The three structural vetoes that make the property true are themselves tested: an unverified same-net zone vetoes the already-connected claim on every layer, the layered adapter refuses a same-net zone outright, and fill must be revision-bound and zone-backed. Deliberately not mitigated: nothing prevents a new surface from adding a geometric pad-in-zone inference; this risk is the record that doing so requires revisiting ADR-0091 first. | Open / adapters |
| R-136 | The net-tie connectivity under-claim is permanent and one-sided: a board whose `net_tie_pad_groups` footprint physically joins two nets (D-179, ADR-0092) always reports those nets unconnected through the tie, because the tie copper converts with `net_id None` and no surface makes a joined-nets claim. A caller acting on "unconnected" could propose new copper joining the tied nets somewhere else on the board, creating an uncontrolled second short that only authoritative KiCad DRC (ADR-0004) would then catch; and a caller comparing KiCad's connectivity report against Board IR's will find them in permanent disagreement on exactly these nets, with nothing in the conversion result saying why. The tie obstacle also over-covers: its envelope can blanket the tie pads entirely, so a route request targeting a tie pad may fail `no_path` at default clearances. | Medium | Medium | Deliberate and bounded rather than accidental: the under-claim is the only direction the connectivity rule permits without an exact proof that the polygon bridges both pad groups, and the over-cover is the direction the obstacle rule requires. Every net-tie board is additionally write-back refused as a whole -- the tie segments carry revision-derived identities and both patch adapters refuse any snapshot containing one -- so no proposal that misreads the tie can be applied through CopperMCP. `tests/test_net_tie_footprints.py` pins the non-claim, the obstacle, the mutation control, and the patch refusal. Exit path recorded in ADR-0092: an explicit connectivity edge becomes admissible once a transform-aware exact bridge check exists. | Open / KiCad integration |
| R-137 | A schema published under `schemas/` can drift from the payload the code emits and nothing notices, because the Python boundary types validate against their own runtime rules rather than against the schema file a third party would load. This is recorded as a risk rather than a closed defect because issue #11 found one instance (`clean`, D-180) and the mechanism that produced it -- two independent descriptions of the same payload, only one of which is executed -- still exists for every schema. The drift has two forms and the second is the dangerous one: a schema that omits a field the code emits makes a **true** payload fail, while a schema that declares a field but not the rule constraining its value lets a **false** payload pass. Review of this change found the second form left open on both `passed` and `clean`, so a schema-only consumer could have accepted and displayed a board as clean that CopperMCP's own runtime refuses to call clean. | Medium | Medium | `tests/test_schema_conformance.py` now carries `_SCHEMA_COVERAGE`, which names every file under `schemas/` and the exact test function that proves it accepts a real emitted payload. Two guards close the loop: a completeness test fails if a new schema file appears with no entry, and a proof-existence test fails if a named test is renamed or deleted, so neither a new schema nor a silently removed proof can leave one unverified. Each of those two guards carries its own guard-the-guard test — `_coverage_gaps` and `_proof_is_defined` are asserted to report a divergence rather than only ever being shown two inputs that already agree — because a guard whose failing direction is never exercised is indistinguishable from one that always passes, and mutating both to a constant confirmed each is killed. For the three schemas with a Python model the check is stronger than a fixture: `_field_parity` compares the schema's declared property names against `to_dict()`'s key set directly, which is the comparison that was never made before, and it has its own guard-the-guard test proving it reports a mismatch in both directions. Against the value-drift form, `schemas/drc-summary.schema.json` now pins both derived flags in both directions with `if`/`then`/`else`, checked against `DrcSummary`'s own answer over a count grid rather than against a restatement of the rule, with four negative fixtures and eight killed constraint mutations; `board-manifest` and `candidate` were checked for the same class and carry no derived value, so there is nothing there to pin. Note the limits, since neither is obvious: `_field_parity` compares field *names* only and is structurally incapable of seeing a value rule, which is why it reported parity while a lying payload validated -- it has a test saying so -- and JSON Schema cannot express the model's remaining requirement that the violation-type counts sum to `error + warning + exclusion + unconnected`. Deliberately not mitigated: two schemas are proved by a weaker claim than an emitted payload and say so in a one-value literal -- `board-ir/0.1.0.schema.json` is `legacy_no_emitter` (the active codec refuses a 0.1 envelope by design, so no live payload exists to check) and `audio-benchmark-catalog/0.1.0.schema.json` is `committed_artifact` (the published artifact is a file in the tree, validated by `scripts/check_audio_benchmarks.py`). Nothing checks that a *new* field added to a model reaches the schema before review; the parity test catches it at review time, not authoring time. | Open / infrastructure |
| R-138 | An `off_grid` refusal that now names a pad, a pitch and a miss reads like a complete diagnosis, and on the measured corpus it is not one. B-100 re-previewed all 18 real-board refusals at exactly the finest lattice their own geometry admits and routed none: 13 are blocked by a pad centre lying outside the board outline inset by half the requested track width and 5 by the grid-node budget, neither of which the evidence mentions. A caller — or an agent — that reads `largest_representable_step_nm` as "set `grid_step_nm` to this and it routes" will act on a claim the payload does not make. The same risk in its disclosure form: the payload is the first routing refusal to carry board-derived geometry, so anything that stores refusals durably now stores geometry unless it is taught not to. | Medium | Low | The field is documented as representability and never as routability in three places a reader will actually meet — the backend-neutral contract, the published MCP contract, and ADR-0093's alternatives, where a `would_route_at_that_step` field is rejected by name as a claim the code cannot prove where it refuses. No field predicts routability, and a non-claim literal is deliberately not added, because a field that only ever says `not_run` is worse than its absence. The durability half is closed structurally rather than by review: `scripts/benchmark_real_board_capability.py` truncates the message to an imported constant, `tests/test_benchmark_real_board_capability.py` asserts the recorded string carries no digit at all, and the mutation that records the message verbatim is killed. | Open / routing |
| R-139 | Board IR models no text and therefore no text-variable map, so the root `(property "<key>" "<value>")` pairs D-184 accepts are read past and counted, never carried. Two consequences follow and neither is prevented by anything structural. A caller that rebuilt a board from a Board IR snapshot alone would silently drop the map, and any board text reading `${KEY}` would then render unexpanded — the value KiCad substitutes is real content in several places — copper text glyphs through `PCB_TEXT`, `PCB_TEXTBOX` and `PCB_TABLECELL`, a `PCB_BARCODE` module pattern, and a `.kicad_dru` custom rule whose clearance the value can supply — so this is a loss of meaning and not only of a label. The accept is sound today because every one of those termini is already refused by or already outside this adapter — root and footprint graphics on copper refuse, `barcode` is not in the root vocabulary, `.kicad_dru` is never parsed here, and the authoritative DRC surface hands the real files to KiCad over write-back-preserved bytes — which makes the risk conditional rather than present: it becomes live the moment any of those four closures is opened. The second form is a KiCad-side surprise this project does not cause and can be caught by: `BOARD::SynchronizeProperties()` replaces the whole map from the project's `text_variables` on save, so a board-only key does not survive a GUI save with a project loaded, and resolution precedence between the two sources flipped after KiCad 9.0. | Low | Medium | The count is published rather than implied: `ConversionResult.unmodelled_board_property_count` states how many pairs were accepted and not modelled, so a caller that needs the map can read the count and decline instead of being told nothing, exactly as `unmodelled_group_count` (R-134) and `max_roundrect_rounding_nm` do. The four closures the accept depends on are named in ADR-0094 and three are pinned by `test_accepting_a_board_property_does_not_admit_the_text_that_expands_it`, which fails if root copper text, footprint copper text or `barcode` is ever admitted — so a future change that opens one of them must re-argue this accept rather than inherit it. The fourth, `.kicad_dru` never being parsed, is pinned by nothing and is recorded as such rather than left to be assumed covered: it is the absence of a parsing path, so it rests on ADR-0005 and on review. A related loss was found by review on a second surface and repaired here: Circuit Scene skipped root properties on the ground that no supported board could carry one, which this decision falsified, leaving an author-controlled string invisible and uncounted; they are now disclosed under a `board_property` origin. Write-back is verified rather than assumed: `test_a_placement_splice_leaves_a_root_board_property_byte_identical` renders a real move and asserts the property's bytes and the whole document tail are unchanged, which is what keeps the authoritative-DRC path honouring the real value. No Board IR schema, digest or golden identity moves, so nothing durable now depends on the gap. | Open / Board IR |
| R-140 | The copper-text refusal is correct today because of a fact about KiCad that CopperMCP cannot observe, and the same fact is what a future acceptance would have to depend on. Nothing in a `.kicad_pcb`, in the KiCad file format, or in this codebase declares which Newstroke glyph table or which installed font will plot the gerbers, so any per-glyph envelope is sound only for the build it was measured against and its refutation is silent: a widened glyph turns a shipped over-approximation into an under-approximation with no signal, no diagnostic and no changed digest. The residual that exists **now**, with the construct refused, is narrower and is a disclosure-free over-refusal: one board in the surveyed corpus is unconvertible for a single dimension annotation a designer left on `F.Cu`, and a caller cannot tell from the refusal that deleting or re-layering that one object would convert the board. The related standing gap is that Board IR never reads the project file at all, so `${…}` in *any* accepted construct — today only on non-copper layers, where it carries no copper — is a quantity outside `source.revision`; that is harmless while such text is unread and becomes load-bearing the moment anything reads it. | Low | High | The construct refuses rather than being modelled, and the refusal is pinned by `test_root_copper_text_refuses_under_its_own_name` over both heads and three copper layers; the mutation that stops applying it is killed. The acceptance boundary is a closed field table in ADR-0095 and is pinned by `test_root_text_on_a_documentation_layer_still_converts`, so a widening is a decision rather than an edit. The four conditions any future acceptance must meet — project variables digested, path- and clock-derived variables refused, `(face …)` refused or carrying freshness-bound outlines, glyph extents pinned by digest, and a font-build mismatch that refuses — are written down in section 5 of the research note so the question is reopened on evidence rather than on appetite. | Open / Board IR |
| R-141 | Mapping `connect` onto `PadKind.SMD` (ADR-0096) **erases the token from the IR**, and the erasure is not recoverable from a snapshot. A consumer reading `kind == "smd"` cannot tell that the designer wrote `connect`; one generating fabrication output from Board IR would put solder paste on an edge-connector finger, would not drop the footprint from a "exclude all TH" position file the way KiCad does, and one reasoning about copper permitted to reach the board edge would apply the wrong rule, because KiCad exempts a `CONN` pad from the Edge.Cuts clearance test and an `SMD` pad is not exempt. At least ten divergences are discarded, in four classes (D-186); none is geometry or connectivity, which is what makes the mapping sound, but the *count* is a lower bound rather than an enumeration and should be read as one. **The disclosure is weaker than it looks, and this is the sharpest half of the risk.** `ConversionResult.edge_connector_pad_count` is an in-process return value: `BoardIrSummary` (`board_ir_service.py:100-181`) has no field for it, so it reaches no MCP contract, no CLI output and no Circuit Scene, and **from an MCP client the discard is silent**. An early draft of ADR-0096 called it "discarded loudly"; that is retracted. The same is true of `unmodelled_group_count` and `max_roundrect_rounding_nm`, so this is a **pre-existing property of the measured-field pattern** rather than a fault this decision introduces -- which makes it worse, not better: three separate decisions now rest on a disclosure channel no published surface carries, and a reader should not have to rediscover that three times. The acceptance is also load-bearing on a property that is true today and enforced by no type: **CopperMCP makes no paste, mask, or fabrication claim, evaluates no user DRC rule expressions, and derives no edge clearance of its own.** A future surface that emitted a stencil or a position file, or that inferred board-edge legality from Board IR rather than from KiCad's DRC, would make the mapping unsound in exactly the directions those discarded divergences describe. | Low | Medium | **The real bound is the write path, and it is proved rather than asserted.** Both patch adapters are source-preserving splices that rewrite only footprint/pad `at` expressions and route geometry, never a pad header, so the `connect` token survives an apply byte-for-byte -- and therefore KiCad's own DRC, position file, Gerbers and user rule expressions all still see an edge connector, regardless of what CopperMCP told its caller. `tests/test_kicad_placement_patch.py::test_a_placement_splice_leaves_an_edge_connector_pad_token_intact` renders a real placement move over a board whose *moved* footprint carries `connect` pads -- the hardest case the splice offers, since it rewrites that footprint's own `at` and every one of its pads' -- and asserts all four tokens survive and the patched board still reports `edge_connector_pad_count == 4`. Nothing CopperMCP writes can turn a finger into an SMD pad. The count remains the in-process signal for a caller that cares, following the D-157/ADR-0090 measured-field pattern rather than a diagnostic every caller of `parse_kicad_bytes` reads as a refusal. The dependency is named at the point of change, in the comment on the kind mapping's use site and in ADR-0096's consequences. `tests/test_kicad_board_ir.py` pins the mapping, the count on zero/one/two pads, the refusal of the copper-less form, and the whole domain of the kind table against KiCad's four tokens; 22 of 23 chosen mutants are killed, the 23rd provably equivalent. **Deliberately not mitigated:** no published surface is widened to carry the count. Doing so is a contract change on `BoardIrSummary` and its MCP schema, it should properly cover all three counts rather than one, and it belongs in its own decision rather than being smuggled into a pad-kind change. Until then the honest statement -- now made in ADR-0096, the architecture contract and the changelog -- is that the count is in-process only. The count is also a number, not a set: nothing says *which* pads were edge connectors, and no surface refuses an operation on a board carrying one. | Open / KiCad integration |
| R-142 | Accepting a courtyard on the layer opposite its footprint's side (ADR-0097) makes a footprint's keep-out **two** regions on two layers, and every future consumer of `Footprint.courtyards` that does not also read `far_side_courtyards` silently under-approximates a keep-out. That is the unsafe direction, and it is the exact failure the old refusal was preventing. The class is broad rather than hypothetical: nine call sites across the legalizer, the placement view, the route-scoring projection, the scene payload, the scene vertex budget and the placement patch each had to be widened by hand, and nothing in the type system distinguishes "read the near set because that is all I need" from "read the near set because I forgot the far one". A second, narrower residual: the two sets must never be pooled into one even-odd region, because the stock feed-through footprints draw their back rectangle *inside* their front one, and a consumer that unions them reads a keep-out on a side that has none. | Medium | High | The dependency is named where it can be acted on rather than only in prose. `Footprint.front_courtyards` and `Footprint.back_courtyards`, and `_PlacedFootprint.on_layer`, are the by-layer accessors a new consumer should reach for, and each carries the KiCad source citation for why the footprint's side is not the question; neither reproduces KiCad's own `GetCachedCourtyard` fallthrough, in which every layer that is not a back layer silently returns the front courtyard. `tests/test_courtyard_far_side.py` pins the load-bearing direction directly: three boards differing only in courtyard layer and footprint side receive the tool's own three verdicts, a named test records that the previous same-side gate would have published `proven_clear` for a real overlap, a moved-proposal test fails if the far-side rings are left behind, and the scene test fails if the far-side field is dropped or folded into `courtyards_nm`. B-101 is the reproducible oracle comparison and refuses to emit an artifact on any contradiction. The pooling hazard is closed structurally rather than by review: line chains are assembled per layer and the circle-disjointness rule runs per layer, each with a same-layer control test that must still refuse. Deliberately not mitigated: nothing prevents a *new* surface from reading `courtyards` alone, and no type makes that a compile error — this risk is the record that adding a courtyard consumer requires deciding, explicitly, which layers it constrains. The source-preserving placement serializer is the one consumer that deliberately does not widen: it still refuses any board carrying a far-side courtyard rectangle, which is the conservative direction and is pinned by its own test. | Open / adapters |
| R-143 | Mutation-testing claims published before ADR-0098 carry more certainty than their records can support, and the error direction is the unsafe one. The scratch harnesses behind all 24 hand-applied runs (~170 mutants, a lower bound; the review of the landing PR tallied more) shared CPython's `(mtime, size)` bytecode-invalidation blind spot: a byte-count-preserving mutant applied or restored within the same filesystem second runs stale `__pycache__` code, so a mutant can silently fail to apply or to un-apply, and a stale mutant poisoning the next invocation registers as a **false kill** -- "0 survivors" is the optimistic side. Full-suite invocations (~1,100 s per mutant) are safe because the writes land in different mtime seconds; fast targeted invocations are exposed. From the records alone: 2 runs are `exposed` (B-089, B-093 state targeted invocations; B-102 is their in-ledger correction), 22 more are `unauditable` (no reproducible run established -- including SEC-134 and SEC-136, whose anchor discipline protects the source but cannot see a stale `.pyc`; D-180, whose mutants are JSON Schema edits outside the mechanism but still unreproducible; and D-184/SEC-135, whose recorded figure is from a cache-purged re-run after the 189-failure incident but whose harness is likewise uncommitted), and none is shown false. The larger exposure is reproducibility: none of those harnesses was committed, so every one of those claims was testimony about a deleted tool. | Medium | Medium | ADR-0098: mutation runs go through committed `scripts/mutation_harness.py`, which purges bytecode caches around every application and restoration, refuses a mutant whose anchor does not match exactly once, and proves kills in both directions; specs are committed under `docs/mutants/` with a mutant-to-killing-test mapping; `tests/test_mutation_harness.py` reconstructs the stale-bytecode defect deterministically and gates every committed spec's anchors in CI. Residual, accepted: the ~170 historical mutants are not re-run -- their anchors have drifted and their classification (`unauditable`/`exposed`) already states everything the record supports; a future record citing a pre-ADR-0098 mutation claim must cite it with that literal rather than as a settled kill count; and nothing physically prevents a future scratch harness, only the standard and this row. | Mitigated / `@seunghyukchoe`; ADR-0098, D-188, SEC-138 |
| R-144 | Accepting seven pad fabrication properties (ADR-0100) rests on a sweep of **one upstream tree on one date**, and its refutation is silent. Nothing in Board IR records that a pad carried `pad_prop_bga`, `pad_prop_pressfit` or any of the other five: the token is read, validated and dropped, so a future KiCad that gives an accepted value geometric meaning makes every board carrying it silently wrong rather than refused. `PRESSFIT` is the most plausible candidate -- its whole definition is a hole-diameter tolerance, exactly the kind of quantity a filler, a drill-aware clearance rule or a router could one day read -- and `HEATSINK` is second, because it already suppresses one DRC test and is already nameable in a `.kicad_dru` that can set a `zone_connection`. Two narrower residuals sit beside it. The **disclosure is weaker than it looks, and this is R-141 repeating rather than a new finding**: `ConversionResult.unmodelled_pad_property_count` records that a fabrication annotation was discarded, but not *which* one, and it is an in-process return value that `BoardIrSummary` has no field for — so it reaches no MCP contract, no CLI output and no Circuit Scene, and from an MCP client the discard is silent. The counter is worth having because a future published summary can carry it; it is not worth reading as a disclosure today. And the **castellated refusal is an over-refusal whose cost is unmeasured**: no corpus board carries one, so nothing here establishes how often a real board would be refused for a construct whose exclusion region this decision declined to approximate. | Medium | Medium | The sweep is committed rather than remembered -- [KiCad pad fabrication property research](../research/kicad-pad-fabrication-property-v1.md) tabulates every consumer with a file and line, so re-running it against a newer KiCad is a diff against a written table and not a fresh investigation. The accepted and refused sets are pinned as a **partition** of the eight tokens KiCad's writer can emit (`test_the_pad_property_constants_partition_what_kicad_can_write`), so a ninth token added upstream makes the assertion fail rather than passing silently into the accepted set -- which is the only mechanism here that fires without a human deciding to look. Mutant PP01, admitting `pad_prop_castellated`, is killed by that test and by nothing else, because the value is refused by its own named branch either way. ADR-0100 names the modelling change that retires the risk -- a `Pad` field, deferred on identity-cascade cost exactly as ADR-0091 deferred `PadZoneConnection` -- and records that `pad_prop_castellated` becomes acceptable in the same change. **Not mitigated:** nothing watches KiCad master, and no test can observe a semantic change upstream that does not add a token. | Open / adapters |
| R-145 | The three phono boards behind issue #153 stay refused, and the blocker now visible behind the custom pad is a pad-level `(property ...)` -- KiCad's `PAD_PROP` -- which refuses today through the unknown-child allowlist with a message that names nothing. That is the fifth instance in two days of one refusal masking the next, and the pattern's cost is that a conversion-count target reads as one gap when it is a queue. Separately, ADR-0100's refusal rests on a `Pad` invariant no type enforces: that `_pad_extent`/`_pad_bounds` and `_pad_core_extent` are the only readers of pad geometry and that they need opposite directions of error. A reader added later that takes the obstacle box for an attachment core would break silently, and would make the bounding-box alternative look admissible. | Medium | Medium | The masked blocker is filed as its own issue with the construct named, rather than counted as progress here, and ADR-0100 records the measurement (12 of 18 before and after) that shows no board was won. The invariant is pinned in the direction that matters: `_CORE_MODELED_PAD_SHAPES` already fails closed for a `PadShape` member it does not know, CP5 mutation-checks that admitting `custom` to the enum is caught, and section 8 of the research note states the three conditions -- all required -- under which the refusal should be revisited. | Open / Board IR; ADR-0100, D-190, issue #153 |
| R-146 | Every real-board number this project publishes decays silently, because the corpus behind them is one designer's **live** working tree rather than a frozen dataset. It is edited during long runs -- three measurements in one week were invalidated by a mid-run save -- and it both grows (12 saves, then 17, then 18) and changes what it contains, so a figure is only true of the corpus state that produced it. Two published figures were found stale by the #116 close-out alone: B-096's 14 routed nets is 0 of 425 today because the designer has since routed them (320 `already_connected`), and B-099's "placement preview's supported subset never binds on this corpus" is contradicted by 156 `refused/illegal_placement` verdicts. Neither is a code regression, and that is exactly the danger -- a stale number reads as a capability claim, and issue #116 spent its whole life with three wrong numbers in its title. Denominators decay too: 18 files hold 17 distinct board contents, so "of 18" and "of 17" are both honest and mean different things. | High | Medium | Bracket every run with a conversion-only sweep recording a sha256 per board and refuse to publish if the digests disagree (the `B-099` survey close-out replay took three sweeps and all agreed); always state the corpus state, the date and which denominator; never put a count in a long-lived issue title or a milestone's completion criterion; supersede dated records with a new row rather than editing them, and note the supersession in the living index a reader actually reaches. | Open / `@seunghyukchoe`; D-191, B-099 |
| R-147 | **Freshness-bound fill evidence is established at one seam and silently absent at the next, so a claim proved where it is produced is not a claim where it is consumed.** Two instances, found by sweeping what *consumes* a routed candidate rather than what mentions fill. First and serious: `AStarRouter.replay` rebuilds its request from the candidate and calls `propose` **without** `verified_fill`, and a `RouteCandidate` carries no fill evidence to rebuild it from — demonstrated on B-021's own fixture, where the fill-aware candidate is 8,000 nm and its replay is 14,000 nm, so the equality check fails. Every downstream verifier goes through `_replay_candidate`, so `preview_route(include_fill_authority=True, include_drc=True)` refuses a legitimate candidate with "candidate does not match a deterministic router replay", and `include_apply_token=True` mints a token whose apply fails identically. The flags are independent booleans with no guard between them. It is latent only because B-105 found the shrink never engages on the real corpus — luck, not a mitigation. Second and minor: the single-layer core still performs no *shape* validation of `verified_fill` (no type, ring ceiling, vertex-type or island-count check) where `_invalid_verified_fill` checks all of them. The general hazard behind both is that a sweep enumerating *mentions* of the evidence sees producers and misses consumers, and sees two gated routers without noticing one gate list is shorter. | Medium | High | The replay gap is filed as its own issue and deliberately not fixed in the branch that found it: closing it means deciding how a candidate carries fill provenance across the replay boundary, and `RouteCandidate` is content-addressed with digests pinned in `tests/test_golden_identities.py`, so it needs its own ADR rather than a rider. Until it lands, do not describe `include_fill_authority` with `include_drc` or `include_apply_token` as a supported combination. Containment and a ring floor are now gated on both routers with five committed mutants; the shape asymmetry is recorded rather than silently carried. When a gate is added to one router, state in the ADR whether the other has it — ADR-0039 listed two rules and never said containment was deferred, which is how that gap survived ADR-0070's review. Never argue two seams agree from a with/without differential: it bounds what evidence adds, not what a consumer fails to receive. | Open / `@seunghyukchoe`; D-192, B-105, [ADR-0101](../adr/0101-fill-currency-is-not-in-the-document.md) |
| R-148 | The excessive-agency suite can stop exercising its authorized path without failing. Every row in it degrades to `not_run` by design when the precondition it needs is absent, which is honest per row and dangerous in aggregate: if `preview_route` and `preview_placement` stop issuing tokens, if `include_apply_token` is renamed, or if the apply gate shuts, all three authorized rows and the permit they establish become `not_run` and the artifact still reports zero failures. Measured, not hypothesised: 90 passes become 76 with an empty `failures` list. The residual risk after mitigation is narrower but real — the permit is evidenced on **one** held-out family (`heldout-audio`), because `coppertone-buffer` affords no genuine capability, so a change that broke the permit only on boards unlike that one would not be caught here. Confinement is likewise established against three named escape routes on whatever filesystem the temporary directory landed on, and not in general. | Medium | High | Three report-level controls, predeclared in the digest-bound catalog and counted in the exit status: the authorized apply was exercised somewhere, it was exercised outside the control family, and every declared escape route was attempted. `test_the_coverage_controls_fail_when_the_authorized_path_is_never_exercised` reproduces the degradation and pins `controls_failed == 2` against an empty `failures` list; `test_the_cli_exits_non_zero_on_a_failed_control` pins the exit status. Two mutants that make the controls always satisfied are killed by that regression (`docs/mutants/2026-08-13-apply-authorization-gate.json`), so the controls are load-bearing rather than decorative. Widening the permit beyond one held-out family needs a project family that affords a real capability — the externally authored `.kicad_pcb` intake issue #110 also asks for. **Amendment 2026-08-14:** there are now **four** controls, not three, and the `controls_failed == 2` figure above still describes the degradation that test reproduces. The fourth, `every-accepted-format-family-is-actually-exercised`, was required by `D-193` when `_run_family` stopped raising on an unconvertible board. The residual named in the last two sentences of this row is **not** closed: the intended fix was measured and failed (0 of 164 third-party boards convert), and it is carried forward with its new blocker as `R-153` below — see [B-110](benchmark-ledger.md) and [D-199](decision-ledger.md). | Open / `@seunghyukchoe` |
| R-149 | **`include_fill_authority` with `include_apply_token` now returns no token, and a caller that treated the token as unconditional sees a behaviour change rather than an error.** The withheld token is the honest outcome -- the old one was guaranteed to refuse at apply -- but nothing in the response says *why* it is absent, and the same silence already covers a non-appliable board. A caller cannot distinguish "apply is off", "this board cannot be applied to", and "this candidate was routed under fill" without reading `fill_binding` and `allow_apply` itself. Separately, **the fill-aware path remains effectively untested against real KiCad zone fill**: every test in this change supplies its islands directly, because `run_zone_fill_authority` needs KiCad, and B-105 measured that the single-layer fill shrink never engages on the corpus -- one of twelve converting boards has an `F.Cu` zone and its pour exceeds `max_fill_vertices`. So the binding is proved against evidence this repository constructs, not against evidence KiCad produced, and the assumption that a KiCad refill is byte-reproducible for one board revision is **unmeasured**. If it is not, a future apply that re-establishes evidence would refuse intermittently rather than wrongly, which is the safe direction but a bad experience. | Medium | Medium | The token gate is the same shape as the existing appliability gate and is pinned by `test_a_fill_routed_candidate_is_returned_without_an_apply_token`, whose non-vacuity half asserts the same board still mints a token without the flag. Every refusal added here fails closed. The KiCad-determinism assumption is load-bearing only for work that has not landed: no shipped path re-derives fill, and the apply boundary refuses instead of assuming. | Open / maintainers |
| R-150 | Raising `max_fill_vertices` to 500,000 (ADR-0104) makes a code path reachable on real boards that nothing has ever exercised there, and the first thing behind it is another ceiling sized for fixtures. This budget meters a board's **total** pour; every consumer of the islands it returns charges the **widest single island** instead -- `routing.layered_board_adapter._MAX_FILL_VERTICES` refuses any island above 4,096 outright, and the single-layer core's `_polygon_touches_rect` walks one ring per pairwise test against `max_obstacle_checks`. The two are not proportional: one corpus board holds 61 % of its 27,622 vertices in a single ring, another 29 % of 59,852 across 96 islands, so no board total predicts either. **14 of 18 corpus boards carry an island above 4,096, the widest 43,889**, and seven of them could not be read at all before this change, so the ceiling was unreachable for them and is now not. Expect `verified fill island is not a bounded polygon` to appear where a budget refusal used to be. Two further questions are deferred with it rather than settled in passing: whether that per-island ceiling should move at all, which needs its own calibration against `max_obstacle_checks` and cannot borrow this one's -- B-105 measured the single-layer fill shrink as a **net loss** on this corpus, 2 connectivity improvements against 6 budget regressions and 0 routes unlocked -- and whether a fill ceiling belongs per-request rather than per-server given it gates a KiCad subprocess, which is a caller-supplied bound on server work and a different security posture from an operator's environment. Both live in the routing path, which is under concurrent change. | Medium | Medium | Filed rather than fixed, as [issue #167](https://github.com/seunghyukchoe/copper-mcp/issues/167): the calibration note names the population split and the counts, `B-108`'s runner reports `boards_with_an_island_over_the_layered_ceiling` on every run so the number cannot go stale unnoticed, and the refusal that surfaces is refusal-side in the safe direction -- an island refused is an envelope kept, never copper dropped. | Open / routing |
| R-151 | The schema accepted-set gate ([ADR-0105](../adr/0105-a-schema-version-moves-with-its-accepted-set.md), `D-197`) is a **floor, not a proof**, and a green run is easy to over-read as "no schema drift". It compares property sets, `additionalProperties`, `required`, `enum`, `const` and union `type`. A schema can therefore change which documents it accepts, at an unmoved version, entirely invisibly to it: a tightened `pattern`, a lowered `maximum` or raised `minimum`, a changed `minItems`/`maxItems`, a re-pointed `$ref`, a new `allOf`/`oneOf` branch whose subschemas carry none of the watched keywords, or a `format` gaining teeth. **Direction of error is the optimistic one**, which is why this row exists: an unseen change reports as *no drift*, so the gate's silence is the flattering side of the defect -- the same shape `R-143` records for a stale mutant registering as a false kill. A second, narrower exposure: the gate's history sweep walks the eight release **tags**, so a schema edited and reverted between two tags is invisible by construction, and a schema published outside a tag is not swept at all. A third: `declared_version` falls back to the filename when a schema pins no `schema_version` `const`, so a file renamed without its content changing reads as a first publication and is silently not compared. **A fourth, and it is the one adversarial review demonstrated rather than predicted: an accepted-set gate cannot enforce a byte freeze.** All 2,046 lines of `schemas/board-ir/0.2.0.schema.json` were rewritten -- reindented, every multi-member `enum` reversed -- and the gate plus all 107 schema tests stayed green, correctly, because the accepted set had not moved. The two **frozen** schemas now additionally carry a sha256 byte pin, which closes that route and the unwatched-keyword route for them; **every other schema under `schemas/` remains exposed to it**, because a schema that is expected to change cannot be byte-pinned without turning each legitimate edit into a pin update. | Medium | Medium | Named in the checker's own module docstring and in ADR-0105 under *What the gate does not own*, so a reader of the failure surface meets the limit rather than inferring it; the working-tree axis catches the common case (an edit landing on `main` between releases) even for a keyword the extractor watches only partially, because any watched key that moves still fires. Widening the extracted keyword set is the way the floor rises, and it is a one-function change with unit tests already in place (`tests/test_check_schema_sets.py::test_each_keyword_is_classified_by_the_direction_it_breaks`). Mutants proving the gate fires in **both** directions on the two real historical instances are committed under [`docs/mutants/2026-08-14-schema-set-drift.json`](../mutants/2026-08-14-schema-set-drift.json), including `FZ1`, which is the byte-freeze route this row's fourth paragraph records. No claim is made that the unwatched keywords are unused in this repository -- that sweep was **not run**. And a second non-claim, added after review: the exemption mechanism now enforces that a tag is a release tag and that a recorded direction is one the comparison observed, but **it does not verify that the ledger row an exemption cites exists or says what the exemption claims**. That is reviewer-owned, exactly as it is in `check_doc_links.py`, and a reader who takes a green run as proof that the four exemptions are correctly *described* is over-reading it. | Open / contracts |
| R-152 | **The new public `include_fill_authority` on `preview_layered_route` will refuse rather than route on most real zoned boards, and nothing in the flag's name says so.** The ordered-layer adapter's `_MAX_FILL_VERTICES` refuses any single island above 4,096 vertices, and the audit **verified** that `_invalid_verified_fill` is reached from `_invalid_request` *before any snapshot work*, so one over-ceiling island becomes a whole-request `invalid_request` -- there is no per-island skip and no degradation to the outline envelope, contrary to what [issue #167](https://github.com/seunghyukchoe/copper-mcp/issues/167) itself describes. `B-108` measured **14 of 18 corpus boards** carrying an island above that ceiling, widest 43,889, and no in-repository board does -- so this repository's own tests cannot observe the outcome most callers will get. The direction is over-refusal, which is the safe side: an island refused is an envelope kept, never copper dropped. What is at risk is a caller's model of the feature, not the board. | Medium | Low | Recorded in [ADR-0106](../adr/0106-layered-fill-authority-is-public-and-bound.md)'s Consequences and in `docs/usage.md` beside the flag, so the expectation is published rather than discovered; `B-108`'s runner reports `boards_with_an_island_over_the_layered_ceiling` on every run, so the count cannot go stale unnoticed. Sizing that ceiling is a calibration against `max_obstacle_checks` that this change deliberately does not ride in on -- it belongs to #167 and `R-150`. | Open / routing |
| R-153 | **The excessive-agency suite's held-out claim still rests entirely on data this repository authored, and the intended fix is not available.** Issue #110 and `R-148` both name the same residual: the permit is evidenced on one held-out family (`heldout-audio`), `coppertone-buffer` affords no genuine capability, and the only externally authored family (`tscircuit-benchmark`) reaches no agency boundary at all because no MCP tool accepts SimpleRouteJson. P1.1 was to close that with a PCBench `.kicad_pcb` family. [B-110](benchmark-ledger.md) closes it negatively instead, and the residual is now **larger and better characterised** than when `R-148` was written. It is not merely that one corpus failed: **0 of 164 real third-party boards convert**, because the Board IR adapter accepts exactly one KiCad board format version (`20260206`) and refuses everything older at `unsupported.version` — and behind that gate the refusal is a layer-declaration refusal, not a geometry one. Any externally authored corpus assembled before KiCad 9 is unreachable for the same reason, so this is a property of the intake surface and not of PCBench. Separately, [ADR-0107](../adr/0107-an-aggregators-licence-does-not-govern-what-it-aggregated.md) removes the aggregated-corpus shortcut for finding one: an aggregator's repository licence is not a determination about its data, and per-item verification is now required. The consequence for every held-out number this suite reports is unchanged and should be read plainly — a change that broke the authorization boundary only on boards unlike the two the suite can actually read would not be caught. | Medium | High | Not mitigated, and deliberately recorded rather than papered over; the suite reports it honestly (`tscircuit-benchmark` carries `accepted_format: false` and 34 `not_run` rows, and the limitation is written up in the evaluation research note). What did land is the half of #110 that does not need a corpus: `_run_family` now records a typed `board_does_not_convert_to_board_ir` instead of raising, so a future family with one unconvertible board degrades rather than aborting the artifact — paired, per `D-193`, with a fourth predeclared control, `every-accepted-format-family-is-actually-exercised`, because none of ADR-0102's three notices a family that silently stops contributing. `test_the_coverage_control_fails_when_an_accepted_format_family_stops_converting` reproduces that state and pins `controls_failed == 1` against an empty `failures` list. **The closing condition is unchanged and now has a stated blocker:** a genuinely redistributable, per-item-verified `.kicad_pcb` family that this server can read — which today means either boards authored at `20260206`, or KiCad 4–7 intake, and the second is a separate slice that nothing here has costed. | Open / `@seunghyukchoe` |
| R-154 | **KiCad DRC counts move run to run on fixed bytes at a fixed commit, so any count published without its comparability is unfalsifiable.** `B-107` ran the corpus runner twice at the same commit over byte-identical boards and nine boards differed only in the `drc` section (`error_count` 936 -> 941; `hole_clearance` 201 -> 202; `tracks_crossing` 4 in one run only). `B-108` corroborates the mechanism -- the counts saturate near per-rule caps (199/200/201, 499/500/502) and which rules fill them varies -- and sizes it: baseline-against-baseline disagreement reaches \|Δ\| 32 against 109 across a real change, a factor of 3.4, so the noise is the same order of magnitude as a signal rather than smaller than one. **This is not `R-146`.** `R-146` records that the corpus is a live working tree whose bytes change under a long run; this row records that the *measurement* moves when the bytes do not, and the two would mask each other -- a corpus edit and a saturation reshuffle are indistinguishable in a bare count. | Medium | Medium | ADR-0109's one-value literal (`single_invocation` / `repeated_agreement` / `repeated_disagreement`) on every benchmark DRC section, with the prohibition that no before/after differential may cite a count that is not `repeated_agreement`; enforced by `require_qualified` at four of the five DRC-recording runners' emission points (the fifth is deferred with its reason: its artifact pins the runner's own digest) and by `scripts/check_drc_comparability.py` over every committed artifact, in `make lint` and CI. Counts published before the policy are qualified by `B-111` rather than retracted. `--drc-repetitions` on the corpus runner makes `repeated_agreement` reachable; it defaults to 1, so the default is honest rather than flattering. **Unmitigated residue, stated:** the N-run characterisation of the distribution has **not been run** (issue #170 step 1, plan item P4.1a), so nobody yet knows which counts are ever `repeated_agreement`; and KiCad's own saturation source has not been read, so the mechanism is inferred from observed caps rather than confirmed. | Open / research |
| R-155 | **A checker's calibration decays the way a corpus does, and three of the six mechanisms landed by `D-202` rest on a recorded number rather than on a derivation.** The CI-budget gate is the sharpest instance: `.github/ci-budget-calibration.json` holds 27 hosted legs sampled over twelve hours plus **n=1** for each release-workflow job, and a sample maximum is not a worst case. Nothing re-reads the API; nothing notices when the suite grows past what was recorded, because the gate compares a budget against *history* and the history does not move on its own. The failure mode is the one that already happened, one level up: the number stays plausible while the thing it describes changes, and the run that finds out is a cancelled release. Two narrower residuals sit beside it. The **golden refusal set covers table values only** -- seven tables, 35 entries -- and the adapter emits roughly eighty more refusal sentences as inline literals that no test pins; a message deleted from an inline `self.fail(...)` is still silent, which is the same class the golden set was built to end, unclosed for the part of the surface that is not tabled. And the **published-row gate's outstanding marker is a text convention**: it fails when stale and when premature, but nothing stops a marker being written with a reason that is untrue, so what it enforces is that *something* was said, not that it was honest. | Medium | Medium | **Every one of the three states its own limit in the artifact a reader reaches first**, which is the mitigation that survives the author leaving. The calibration file carries a `not_claimed` field saying in its own words that it is a sample rather than a distribution with a tail, and an `update` field naming the API call and the rule; `docs/development.md` repeats the procedure as prose. The **half-rule margin is the mechanical cover** for the gap between a sample maximum and a real worst case -- it is a factor of two, chosen because the release workflow's own comment already stated it, and it is deliberately not a percentile because nobody has measured one. Two structural choices narrow the decay: *every* recorded observation binds rather than only the newest, so lowering a budget requires deleting evidence rather than outliving it; and a calibration entry that matches no declared budget fails, so the file cannot quietly become a record of a workflow that no longer exists. For the golden set, `test_the_registry_covers_every_tabled_message_in_the_module` scans the module and asserts a **floor** on what it finds, so a new closed table cannot be added without joining the set and a predicate that stopped recognising a refusal sentence fails instead of reporting an empty scan. **Not mitigated:** nothing watches CI durations between pull requests; no test can observe that a recorded run is no longer representative; the inline refusal sentences are deliberately out of scope, because pinning them would make the whole prose surface contractual -- the thing the golden set's own docstring says it is not doing; and the marker's truthfulness is reviewer-owned by construction, in the same way `R-143`'s citation rule is. | Open / process |
| R-156 | **Two published placement legality verdicts over-approximate a claim that requires under-approximation, on `main`, today.** `placement/legalizer.py::_outline_containment` (`:501-518`) and `::_keepout_respect` (`:520-536`) both read `_PlacedPad.bounds` — built from `pad_bounds`, which over-approximates by contract — and publish **`violated`** from it. Enlarging a box makes `rect_inside_ring` more likely false and `rect_touches_ring` more likely true, so both can report a legality violation KiCad does not share, which ADR-0075 and ADR-0080 classify as a false claim rather than a conservative one. The magnitude today is a roundrect's box corner against its rounded corner — the same order `_pad_overlap`'s docstring already reports as measured on this repository's own board — and it is bounded because `pad_bounds` is tight for `RECT`. It stops being bounded the moment a pad's envelope and core diverge, which is exactly what P3.3 proposes. A third site, `::_resolve_bounds` (`:603-617`), is worse than mis-directed: it feeds `rect_inside_ring` and `rect_touches_ring` from the same call and `_centre`/edge-offset from the same result, so it needs three different regions and no re-point satisfies it. **Whether any real board is currently mis-verdicted is unmeasured** — the mismatch is established by reading the predicates, not by finding a board that trips it, and that measurement is the obvious next step. | Medium | Medium | Enumerated, with file:line and the failing predicate, in the [pad geometry reader survey](../research/pad-geometry-reader-survey-v1.md) §4, and `D-203` makes fixing them item 1 of the revisit list explicitly *independent* of custom pads, so the fix is not gated on a schema decision that may never be taken. The proposed shape is the three-valued answer `_pad_overlap` already has — `proven_inside`/`violated`/`inconclusive` — which is why it is worth doing on its own merits. Until then the direction of error is at least *recorded* rather than assumed correct, which is the state `R-145` was opened to end. P3.3 is blocked behind this by `D-203` rather than proceeding over it. | Open / placement; D-203, B-112, issue #116 |
| R-157 | **The blocker standing between this project and an externally authored KiCad family is geometry coverage, not licensing and not the format era — and the two candidate fixes named so far are both wrong.** [B-114](benchmark-ledger.md) took ten open-hardware boards from ten single-author or single-organisation repositories, verified each licence per item under [ADR-0107](../adr/0107-an-aggregators-licence-does-not-govern-what-it-aggregated.md) (8 of 10 importable on licence alone; two carry the permission inside the board file's own `title_block`), re-saved each in KiCad 10.0.5, and converted. All ten clear the board-format-version gate that refused 164 of 164 in [B-110](benchmark-ledger.md). **None of the ten converts.** Eight refuse `Edge.Cuts` outline arcs; with that one gate lifted in a scratch probe, still none converts and six land on [ADR-0095](../adr/0095-copper-text-has-no-derivable-envelope.md)'s copper-text refusal — the one `#141` was closed on with five exit conditions, none met. Two consequences. **First, [R-153](risk-register.md)'s stated blocker is wrong in its second half**: it says an import needs "either boards authored at `20260206`, or KiCad 4–7 intake", and a KiCad 10.0.5 re-save clears the era gate for free, so a slice spent on KiCad 4–7 intake would buy an import nothing. **Second, the obvious next slice is measured not to work**: `Edge.Cuts` arc support is the natural thing to reach for and it converts zero additional boards. The standing danger is that either is attempted on the strength of the refusal message alone. | Medium | Medium | The measurement is the mitigation and it was taken before the work rather than after: `B-114` records both passes, the per-board digests, and the geometry counts (97 curve primitives across 9 boards, 24 copper-text objects across 7), so a proposal to lift either gate can be checked against a number instead of against a refusal string. `benchmarks/corpora/README.md` records the ten determinations so the next slice does not re-litigate them. **The residual is unchanged and stays owned by `R-148`/`R-153`:** no externally authored `.kicad_pcb` family exists, and the excessive-agency suite's held-out claim still rests entirely on data this repository authored. Nothing here narrows that; it renames the blocker. | Open / `@seunghyukchoe` |
| R-158 | Accepting and discarding a pad `thermal_bridge_angle` can be mistaken for a promise that a Board IR snapshot reproduces KiCad's thermal-spoke geometry or refill connectivity. It cannot: the override is absent from `Pad`, and a fill reconstructed from Board IR could use a different shape-dependent default. Connectivity and KiCad parity have no safe approximation direction, so any such promise would be false rather than conservative. The count also identifies only quantity, not which pads carry the field. | Medium | High | No CopperMCP path refills from Board IR. Conservative routing keeps the entire zone outline as an over-approximating obstacle; exact-fill routing consumes only freshness-verified KiCad polygons generated from the original source bytes; malformed values fail closed; route and placement mutation tests preserve the exact token; and `inspect_board_ir.unmodelled_counts.unmodelled_thermal_bridge_angle_pad_count` exposes the loss. Documentation explicitly disclaims snapshot-only refill and round-trip parity. A Board-IR-only consumer that needs exact fill must decline when the count is nonzero. | Open / Board IR and KiCad integration; D-205, B-115, issue #186 |
| R-159 | The three-valued placement repair closes R-156's false-`violated` direction but introduces a result callers can over-read: `inconclusive` means only that neither direction-typed region proved its endpoint. It is previewable under the established “nothing proven illegal” candidate rule, so it can include a pad whose box clips a diagonal edge, a bounds-only keepout contact, or copper wholly remote outside the board. The last case is faithful to KiCad 10.0.5's edge-clearance provider — it checks collision/proximity to `Edge.Cuts`, not global containment — but it is not a fabrication-containment proof. Applying or fabricating solely because `PlacementLegality.legal` is true can therefore exceed what the evidence establishes. Exact custom-pad geometry will narrow some bounds/core gaps but cannot turn global containment into a KiCad edge-parity claim. | High | Medium | The public fields say `inconclusive`, documentation defines it as absence of proof, apply re-validates rather than discards the legality record, and consumers needing an all-proven placement can require `proven_inside` plus `proven_clear` or request candidate-bound KiCad DRC. Real-KiCad fixtures pin bounds-only outline/keepout contact to no false violation and existing crossing fixtures pin the opposite direction. B-116 repeats only the two relevant KiCad violation-type counts twice per converting board and records exact agreement. A future strict fabrication-containment rule must use a distinct name and oracle; it must not silently reinterpret `outline_containment`. Residual: no policy bit currently forces all-proven evidence before preview or apply, and KiCad DRC does not certify copper lies within the fabricated region. | Open / placement consumers; D-206, B-116, ADR-0110, issue #187 |
| R-160 | Board IR 0.4 gives a custom pad two deliberately different regions. A caller that treats the containing copper envelope as an attachment core can claim connectivity through empty space; a caller that treats the anchor as the obstacle can miss real primitive copper. Neither mistake becomes safe because the other direction is conservative. Arbitrary-rotation AABBs and arc circumcircle bounds can also over-refuse legal routes, while the deliberate anchor-only attachment model can miss an attachment that KiCad permits on primitive-only copper. Scene consumers may misread local envelope coordinates as board coordinates, or infer exact custom-polygon/KiCad parity from an AABB. | High | Medium | Shared accessors route obstacle readers to the envelope and attachment readers to the anchor; legalizer exact readers use footprint origins or pad centres rather than either box. `Pad` enforces that the envelope contains the full anchor. Adapter, codec, schema, routing, placement, layered verification, scene-contract and mutation tests cover both error directions. Scene envelope, frame and model fields are all-or-none and label the frame `pad_local`. The grammar is closed and malformed/unknown primitives fail before snapshot publication. Documentation disclaims exact primitive, refill, DRC and fabrication parity. Residual: the AABB may be materially loose, attachment on primitive-only copper is intentionally incomplete, and every future reader must still select and test its direction explicitly. | Open / Board IR, routing and placement; D-207, B-117, ADR-0111, issue #116 |
| R-161 | The production external-candidate disposer has synthetic vertical-slice evidence, not corpus calibration. One accepted fixture and four typed perturbations prove that the seam can decide both directions, but they do not establish that all 70 B-088 routes can be represented without drift, that the budgets fit that population, or that an external proposer sees the same geometry semantics. The core-only export may also be over-read as a user-facing verification product even though no MCP/CLI intake, KiCad DRC, physics signoff or physical validation exists. | High | Medium | Keep issue #99 open and the roadmap item partial. The next benchmark must re-express all 70 B-088 routed nets under the closed v1 document, bind source and result digests, require every unperturbed route accepted, and require the four predeclared perturbations to retain distinct codes. The result contract states `physical_validation: not_run`; no geometry, apply token or mutation authority is returned; MCP and CLI remain unexposed. Follow-on repair and signoff work stays parked behind this evidence. | Open / routing verification; ADR-0112, D-209, SEC-150, issue #99 |
| R-162 | A multi-path external patch can be mistaken for a structurally complete route merely because every individual edge clears obstacles, or B-120's 70/70 result can be read as authoritative physical validation. | High | Medium | V2 is a distinct closed schema; every candidate edge is metered and checked, then exact under-approximating track cores must join every coordinator-derived pad component and every submitted path. Disconnected geometry refuses. B-120 digest-binds all 70 inputs and results and stores no geometry; its four perturbations are mandatory and distinct. The artifact and result contract state `physical_validation: not_run`, and #99 remains open for KiCad DRC and public intake. | Mitigated for Board-IR structural disposal / authoritative DRC, MCP/CLI exposure, electrical and fabrication validation remain open; ADR-0113, B-120, SEC-151 |
| R-163 | The non-replay serializer needed for foreign geometry could become a general replay bypass, or `physical_validation: completed` could be read as a clean/reproducible/fabrication verdict. | High | Medium | The ordinary serializer and all existing callers retain exact router replay. The alternative is private and selected only by `run_disposed_route_candidate_drc` after exact accepted-disposition type and candidate-ID binding checks; it still verifies source/profile/snapshot equality, candidate identity, budgets and exact Board IR round trip. Structural refusal and missing typed DRC consent execute no KiCad process. The absolute preview deadline is propagated through context capture, serialization, the subprocess and post-run validation, and evidence is refused if completion crosses it. Output carries the existing candidate/source/patched-board/context hashes, the full redacted summary, and `single_invocation`; `completed` names execution only. Seven mutation discriminators pin the gates. | Mitigated for the internal continuation. Python in-process privacy is not an authorization sandbox, MCP/CLI remains absent, KiCad DRC is not SI/PI/EMC/thermal/DFM/fabrication/hardware evidence, and reproducible differentials still require repeated agreement; ADR-0114, SEC-152, issue #99 |
| R-164 | A public external-route intake could let hostile geometry exhaust work, echo proprietary coordinates through framework errors, accept forged identity or budgets, disclose a reconstructed patch, or be mistaken for repair/apply authority or a clean/fabrication verdict. | Medium | High | ADR-0115 gives MCP an exact `{request}` wrapper and a closed `1.0` inner envelope while leaving runtime parsing to the fixed-diagnostic application boundary. The selector is reference-only with both revision preconditions; v1/v2 documents remain closed; candidate identity, net binding and metrics are server-derived. Obstacle and edge ceilings derive only from the bounded coordinator settings, with no standalone budget fields and no budget or settings inside the foreign document. DRC is mandatory, structural refusal runs none, and the closed result carries only bounded counts, recomputed identity and digest-bound aggregate evidence. Transport tests must pin both inventories, annotations, schema closure, non-echoing errors and a forbidden-property leak scan. Documentation separates `completed`, `passed`, `clean` and `single_invocation`. | Mitigated only when the transport tests and full local gate are green. No CLI, persistence, geometry export, repair, apply or live IPC exists; authoritative DRC is not SI/PI/EMC/thermal/DFM/fabrication/hardware signoff, and one invocation is not a reproducible differential; ADR-0115, SEC-153, issue #99 |
| R-165 | Widening the layered per-island fill ceiling can turn one caller-supplied ring into repeated bounds scans and dict-heavy canonical identity allocation before the route search work budget exists; deleting the ceiling would let the 10,000,000 aggregate limit buy multi-gigabyte transient work. | High | Medium | ADR-0116 separates source preparation from rectangle search and selects a fixed 500,000 ceiling only after subprocess-isolated proposal/replay calibration. Exact 500,000 passes; 500,001 refuses before `_points_bounds` or `fill_binding_for`; aggregate overflow retains its separate typed budget code; rectangle envelopes remain charged to `max_obstacles` and route relations to `max_obstacle_checks`. Five new committed mutants pin the per-island ceiling, both failure mappings and obstacle billing; the byte-identical historical #189 spec separately retains five aggregate/call-site/message mutants. | Mitigated for the measured host and bounded contract. Timings are not cross-machine guarantees, operator-raised parse limits do not widen this route seam, and no route-quality, DRC, electrical, fabrication or hardware claim follows; ADR-0116, B-123, SEC-154, issue #167 |
| R-166 | An opt-in local repair can be over-read as trusted because its abstract path verifies while its window or blocked cells omit Board IR or same-pass copper, its work is unmetered, or a partial replacement escapes after cancellation or failed physical clearance. The internal projection can also drift from reference A* obstacle semantics. | High | Medium | ADR-0117 derives immutable provenance from the verified snapshot and rejected allocation, checks area before cell expansion, includes track width and resolved clearance for same-pass copper, rechecks canonical integrity before constructing a local request, and meters projection/local/validator work. Publication requires candidate-path validation, repair-bound reidentification and a whole-set physical recheck; every failure publishes no partial allocation. Focused parity tests pin representative Board IR obstacles to the reference router. | Mitigated for the internal transaction. The one-window transaction may over-refuse, its private projection adapter must move with the reference obstacle model, and no held-out quality or physical claim has been measured; ADR-0117, D-214, SEC-155, issue #90 |
| R-167 | A surrogate, caller-selected backend, or future consumer could mistake candidate-bound SI/PI/thermal/DFM evidence metadata for authoritative signoff, or forge a positive claim without a fixed backend's completed content-digest-bound output. The absence of an executor could also be hidden by a nominal `SIGNED_OFF` status or by leaking backend details through failure results. | High | High | ADR-0118 keeps the private seam structurally non-claiming until a coordinator-owned bounded executor and fixed backend registry exist. Production returns only typed refusal or explicit non-claim; surrogate/advisory output cannot approve; positive evidence must bind one immutable candidate and Board revision plus the authoritative output content digest. Closed inputs, fixed diagnostics, redacted failures, cancellation/deadline handling and no MCP/CLI exposure prevent the deferred capability from becoming a public or unbounded authority. | Open / authoritative-signoff architecture; D-215, ADR-0118, SEC-156, issue #91 |
| R-168 | A per-net SimpleRouteJson conversion can be mistaken for validation of the complete routed output, or permissive relation/number parsing can bind one trace to the wrong imported net or alter geometry before the production disposer sees it. | High | Medium | The adapter remains in the benchmark import package and is unreachable from tools, server and production routing. It replays exact source bytes and snapshot identity, compares the full non-trace source projection, preserves literal JSON number tokens, rejects duplicate keys and number strings, resolves only exact declared aliases and point identities, and refuses zero/multiple ownership. Fixed byte, trace, relation, per-trace, aggregate route-item, numeric-token and extent ceilings bound parsing. The accepted subset is integer-nanometre, positive-width, uniform-width, single-layer, rectilinear and wire-only; v1 requires exact endpoints and v2 requires all imported pads in one acyclic tree before the unchanged disposer reconstructs identity and checks Board IR. | Mitigated for the import-side adapter only. Separate per-net conversions do not prove candidate-to-candidate clearance; via/pad/trace DRC, full-output validation, production exposure, KiCad continuation and any apply authority remain absent; D-217, SEC-157, tscircuit #1964/#2058 |
| R-172 | A caller can mis-handle an absent apply token when several causes collapse to `null`, while a free-form explanation or inconsistent output contract can disclose board details or admit an ambiguous token-plus-refusal document. | High | Medium | ADR-0120 defines one eight-value leaf vocabulary and one precedence function for all six preview surfaces. Values contain only fixed lowercase words and never interpolate caller or board data. Every serialized result carries exactly one of token and reason; internal dataclasses/dict builders and strict MCP output models independently enforce the exclusive-or relation. A formerly swallowed placement replay exception maps to fixed `replay_refused` without exception text. Fifteen committed mutants pin reason branches, surface coverage, closure, and the three public-contract gates. | Mitigated for preview outputs. The reason is diagnostic only and does not grant apply, live mutation, persistence, DRC, electrical or fabrication authority; D-221, ADR-0120, SEC-159 |
| R-173 | A private, actively edited corpus can drift during an applicability census, and a public result can leak proprietary board identity even when board bytes are omitted. A single “appliable” total can also launder distinct route and placement gates into one false capability claim. | High | Medium | B-128 fixes the selection at exactly 18 saves with two explicit existing superseded exclusions and the established history/derived exclusions. Sources are descriptor-anchored, workspace-confined, suffix-checked and byte-bounded; all 18 digests are re-read and compared after measurement. The runner captures its own bytes before measurement and, before output, rechecks unchanged HEAD, clean worktree state and identical runner bytes. The public artifact contains only aggregate verdicts, a 128-bit-truncated aggregate fingerprint, runner/self digests, commit and cleanliness; tests reject paths, board names, per-board digests, geometry, bytes and apply tokens. Route and placement outcomes remain separate. | Mitigated for this frozen census. The private corpus is not redistributable, its aggregate fingerprint is not a public fixture, and results do not generalise beyond these bytes or establish apply success, DRC, live IPC, electrical, fabrication or hardware acceptance; B-128, D-222, SEC-160 |
| R-174 | A `dfm` `SIGNED_OFF` result is read as manufacturability, or as authorization to write copper. It is neither: it means KiCad DRC was run N >= 2 times over one exact candidate on one exact revision, agreed with itself, skipped nothing, and found no errors and no unconnected items. It says nothing about SI, PI, thermal, assembly, panelisation, yield, or any rule the loaded DRC profile does not encode; and the three unregistered domains sharing the word "sign-off" make the narrower meaning easier to over-read, not harder. | Medium | High | The claim carries what it rests on and only that — domain, candidate, revision, evidence digest, comparability and repetition count — so a consumer can see the scope rather than infer it. The seam is not exported through MCP or CLI, so nothing reaches a model or an operator through a public surface yet. The module-private evidence capability is a cooperative internal-misuse guard, not an in-process sandbox against a privileged Python caller importing or monkeypatching private symbols. `apply_candidate` and `apply_placement_candidate` remain separately authorized and are untouched by sign-off, so a claim cannot become a write. Unregistered domains answer `no_authoritative_backend` rather than a silent absence. ADR-0119 states the narrow meaning in the words a reader will meet it in. **What is not mitigated:** the gap between "DRC found nothing" and "manufacturable" is real and is not closed by anything here; closing it needs the DFM adapter issue #91 asks for. | Open / routing |
| R-175 | A fixed-point masking census can be over-read as a conversion forecast even though deleting an unsupported construct is deliberately unsound, or a generic locator-driven mask can erase arbitrary board semantics until a file appears to convert. A private cohort fingerprint can also be mistaken for a redistributable manifest or leak board identity through per-item output. | High | Medium | B-129 admits only three exact direct-root CST classes already named by B-114/B-118 and removes the whole class in immutable memory. Fixed diagnostic/locator pairs select those classes; setup, layer, footprint, courtyard, byte-offset, nested and unknown constructs stop. The manifest is out of tree, bounded, no-follow, closed, digest-bound and independently fingerprint-pinned; the artifact contains only public/private aggregate depths and closed terminal classes, with no per-board row or digest. Source bytes are captured once and re-read unchanged. | Mitigated for this counterfactual runner. The result is an upper-bound blocker map, not support, correctness, conversion, DRC, fabrication or representativeness evidence; B-129, D-224, SEC-162, issue #188 |
| R-176 | Holding `mcp` below 2.1 freezes a dependency whose newer release the project has not adapted to, and it conceals a weakness the upgrade exposed: on the pinned 2.0.x line `MCPServer.call_tool()` collapses *every* escaping exception into `ToolError`, so the excessive-agency harness's `BOUNDARY_EXCEPTIONS = {"ToolError", "ValidationError"}` check cannot distinguish a deliberate boundary refusal from an unhandled crash deep in a parser — the exact conflation `_bounded()` documents itself as preventing. Its 15 passing `budget_dos` cases are therefore evidence that the call failed, not that it was refused. | High | Medium | The pin is recorded with its reason in `pyproject.toml` and `D-225` rather than left as a silent range edit, and CI resolves the same 2.0.x surface the committed artifact was produced under. The refusals themselves are real — `request_boundary.RequestError` is raised deliberately at the boundary and 2.1 mislabels it only because it does not subclass mcp's `ToolError` — so this is a weakness of the *evidence*, not a demonstrated hole in the product. | Mitigated / MCP. The four steps this row named as the closing condition were all taken: the range moved to `>=2.0.0,<2.2.0`, `mcp_server._ANTICIPATED_REFUSALS` raises 16 audited refusal types as mcp `ToolError` at the adapter by explicit class, the excessive-agency evaluation was re-run under the stricter classification, and ADR-0121 records the full per-type audit including the 15 types deliberately left to crash. What actually changed is recorded rather than assumed: the committed artifact's counts do not move — 136/90/0/46, byte-identical on both lines — and the 15 `budget_dos` cases pass for a different reason than before, because the harness can now tell `ToolError` from `UnexpectedToolError`. `BOUNDARY_EXCEPTIONS` is unchanged and `UnexpectedToolError` is never admitted to it. Two of the five `budget_dos` scenarios only passed once a *source* defect was fixed: `validate_candidate` and `compare_candidates` refused untrusted input with a bare `ValueError`, which 2.0 could not distinguish from a crash either. **What is not mitigated:** the distinction holds for the audited enumeration and for the two resolutions the suite is run against, `2.0.0` and `2.1.1`; a refusal type added later without an audit entry still reaches a 2.1 caller as a crash, and nothing here is a claim about `mcp` 2.2 or 3.x; `D-226`, `ADR-0121`, `SEC-163`, `D-225` |
| R-177 | A server-side invariant violation on a read-only surface still reaches an `mcp` 2.1 caller dressed as a deliberate refusal. `ADR-0121` translates 16 audited types to `ToolError`, and seven of them also raise from the `__post_init__` of a class the *server* built rather than the caller supplied — `RoutePreview` (31 sites), the four placement result classes (18), `BoardIrSummary` (13), `RouteBundlePlan`/`RouteBundlePreview` (12), `SceneAnnotation`/`WithheldKind` (4) and `LiveEditorContext` (3). When one of those fires it is a defect in this code, not an answer to the request, and the model is told otherwise. | Medium | Medium | The worst case is already closed: `ApplyResultInvariantError` carries the 15 sites that could fire *after* an authorized write, so no post-write inconsistency can be reported as a refusal — the apply pair is the only surface in the project where a misclassification could misstate whether the caller's board changed, and mutants `RC14`–`RC17` pin that split. The residue is bounded to read-only proposals and observations, where a misclassification misreports server health and nothing else, and it is not a regression: on the 2.0.x line every one of these already reached the caller with its text. Splitting them is not a rename — the same modules use the identical `__post_init__` shape for genuinely caller-decoded classes (`RoutePreviewRequest`, `PlacementIntent`, the seven placement rule classes, `models.CandidateSummary`/`DrcSummary`/`ErcSummary`), so each class needs its own construction audit against ~100 raise sites and 100+ existing type assertions. | Open / MCP. Deliberately scoped out of `D-226` rather than swept: the census is recorded in ADR-0121 so the follow-up is fully specified, and `ADR-0121` states the bound explicitly rather than claiming the classification is complete; `D-226`, `ADR-0121`, `R-176`, issue #217 |
| R-178 | Board IR now reads past a board's physical stackup, its drill-and-place origin, its editor grid origin and its solder-paste stencil defaults, and reports each only as a number. A consumer that rebuilds a board or generates fabrication output **from a snapshot alone** loses all four: the drill/place output lands in the wrong frame, the stencil apertures revert to KiCad's defaults, and a board's whole physical stack — layer order, thicknesses, materials, dielectric constants, loss tangents, surface finish — is gone, so no impedance, delay or thickness claim can be made from Board IR at all. The stackup erasure is the largest of the four: B-130 measured 86 layer entries across six boards, and none reaches a caller as anything but a count. A second, narrower residual sits behind it: the stackup is sound to read past **only while** post-machined pads and vias are refused, because in KiCad 10 a counterbore or countersink derives its per-layer copper knockout from `BOARD_STACKUP::GetLayerDistance()`; accepting either construct later without modelling the stack would silently become an over-report of copper. | Low | Medium | Two typed disclosures rather than silence: `unmodelled_setup_field_count` and `unmodelled_stackup_layer_count` reach every MCP client through `BoardIrSummary.unmodelled_counts`, and the second is deliberately comparable against `len(copper_layers)` so a caller can see the physical entries it never received. The non-claim is stated in the migration note and in [ADR-0122](../adr/0122-the-stackup-is-read-per-field-because-three-of-its-fields-are-not-about-z.md) rather than left to be inferred. The post-machining premise is pinned by tests on both pads and vias, so the unsound combination fails a test before it ships. Modelling the stack is the named exit, and it is the one change here that *would* move the Board IR schema under ADR-0105; nothing measured today needs it. | Open — `@seunghyukchoe` ([D-227](decision-ledger.md), [B-130](benchmark-ledger.md), issue #188) |
| R-179 | Board IR now reads past a footprint's schematic provenance and its per-footprint solder-mask and solder-paste defaults, reporting each only as a number. A consumer that rebuilds a board or generates fabrication output **from a snapshot alone** loses both: stencil apertures and mask openings revert to the board defaults, and the footprint's originating sheet — which a designer uses to reason about a hierarchical design, and which KiCad's own repeat-layout tooling depends on — is gone. A second, sharper residual sits behind `sheetname` and behind footprint-local `group` membership: both are *selectors* usable by a custom DRC rule (`memberOfSheet()`, `hasComponentClass()`, `memberOfGroup()`), so discarding them destroys the input a `.kicad_dru` rule would use to raise a clearance above the netclass value. That exposure is **not new** — CopperMCP does not parse `.kicad_dru` at all, and never has, so any rule-derived clearance is already invisible to it — but this decision widens the set of board content whose only route to copper runs through that unread file, and saying so is the point of this row. Third: the footprint-level `zone_connect` override is consumed by KiCad *after* custom-rule iteration, unlike the pad-level one, so a rule can override an accepted attaching mode; the acceptance stays sound because a rule can detach any pad regardless, but the pad-level guarantee must not be assumed here. | Low | Medium | A typed disclosure rather than silence: `unmodelled_footprint_field_count` reaches every MCP client through `BoardIrSummary.unmodelled_counts`, and footprint-local groups join the existing `unmodelled_group_count` rather than vanishing into a second, unread number. The non-claims are stated in the [migration note](../migrations/footprint-field-acceptance.md) and in [ADR-0123](../adr/0123-a-container-refusal-that-names-no-field-is-the-defect.md) rather than left to be inferred. The one field whose value genuinely governs copper clearance, `clearance`, is **refused** rather than counted, and a detaching `zone_connect` is refused too, so no accepted head can under-report copper by itself. The `.kicad_dru` gap is the named exit and is unchanged by this decision; modelling any of these fields into `BoardIRSnapshot` is the other, and it is the one change that *would* move the Board IR schema under ADR-0105. Nothing measured today needs either: B-133 records `unmodelled_footprint_field_count` at 0 on all 15 converting boards of the frozen own corpus. | Open — `@seunghyukchoe` ([D-228](decision-ledger.md), [B-132](benchmark-ledger.md), issue #188) |
| R-180 | The `Edge.Cuts` outline arc admitted by [D-229](decision-ledger.md) is modelled as an inscribed polygon, so the board is modelled **smaller** than it is drawn — by at most the declared 5,000 nm sagitta. That is the safe direction for routing room and for a `proven_inside` containment verdict, and it is the *unsafe* direction for any claim that needs the boundary in both directions. The bound is published as `ConversionResult.outline_inward_deviation_nm` and the one surface that publishes a two-directional outline claim, `placement_preview`, refuses on it. **The residual is that the number does not survive the Board IR document.** A caller who takes a snapshot from `inspect_board_ir`, persists its JSON, and feeds it back into a scene, a parity check or an ERC run has a snapshot whose outline is inscribed and which says nothing about it — because Board IR 0.4 has no field for it and D-229 declined the schema move on measurement. The sharpest instance is `circuit_scene`'s `around_ref` bounds index, which documents a requirement to **over**-approximate and would under-approximate an inscribed outline's bounding box by up to the same 5,000 nm, so a region window touching only that sliver at a rounded corner could report the outline absent. A second, smaller residual: a board whose arcs exhaust the ring vertex budget is silently modelled more coarsely rather than refused — the deviation grows and is reported, but a caller reading only the ring sees a coarser board with no separate signal. | Low | Medium | The bound is **measured rather than asserted**: it is computed with the radius rounded up and the polyline's closest approach rounded down, so it can overstate and never understate, and a unit test samples the true arc densely and fails if it ever understates. Every surface that can reach the number through a live conversion does: `placement_preview` refuses, and routing is sound in this direction by construction. The exit is the schema move ADR-0124 records as the alternative — carrying the arc exactly, or carrying the deviation, on `OutlineContour` — and it is deliberately gated on a consumer that needs the number rather than the flag. Nothing measured today needs it: B-135 records `outline_inward_deviation_nm` at **0 on all 15** converting boards of the frozen own corpus, because no board there draws an `Edge.Cuts` arc. | Open — `@seunghyukchoe` ([D-229](decision-ledger.md), [B-134](benchmark-ledger.md), issue #188) |
| R-181 | Board IR now converts a footprint's stray copper polygons into obstacles that are **looser than the copper they stand for** ([D-230](decision-ledger.md), [ADR-0125](../adr/0125-stray-footprint-copper-is-bounded-because-no-fill-rule-is-written-down.md)). The envelope is a proven superset, so it can never permit an illegal route; the residual runs the other way. **First: a routable board can report unroutable.** A concave or self-intersecting shape — 54 of 56 and 26 of 56 respectively, by B-136 — is replaced by its bounding box, so channels the real copper leaves open are closed to the router. **Second: the board becomes write-back incapable.** An `fp_poly` is not a track, so the emitted `Segment` carries a revision-derived identity and both source-preserving patch paths refuse the whole snapshot. A caller can convert, inspect and route such a board and cannot apply the result — the same contract net-tie copper has had since ADR-0092, now reachable on a much larger class of board. **Third: a caller reading a snapshot alone cannot recover the artwork.** The polygon's vertices are gone; a consumer regenerating fabrication output from Board IR would emit a rectangle where the board has a logo. | Medium | Medium | A typed disclosure rather than silence: `footprint_copper_graphic_envelope_count` reaches every MCP client through `BoardIrSummary.unmodelled_counts`, and a non-zero value is exactly the statement "this many obstacles are looser than the copper they stand for". The magnitude is bounded by measurement rather than by hope — B-136 found **every** envelope under 5% of its board's `Edge.Cuts` bounding box and each board's whole envelope union under 5% too — and the ratios are recorded in buckets so a future cohort can be compared against them. The write-back refusal is a *guarantee* rather than a defect: it is what stops a patch silently deleting copper it cannot represent, and it fires on the whole snapshot rather than per object. The alternative directions are worse and are recorded as such in [ADR-0125](../adr/0125-stray-footprint-copper-is-bounded-because-no-fill-rule-is-written-down.md): an exact model needs a fill rule the document does not carry, and refusing the board answers "is there an obstacle here" with silence. The named exit is a Board IR obstacle type that can carry a non-simple region, which would move the schema under ADR-0105 and is not needed by anything measured today. | Open — `@seunghyukchoe` ([D-230](decision-ledger.md), [B-136](benchmark-ledger.md), issue #188) |
| R-182 | **Evidence taken from a live editor session cannot be replayed, and this project has now produced some.** [B-138](benchmark-ledger.md) and [B-139](benchmark-ledger.md) are the first records here whose measurements exist only because a particular editor was running on a particular machine at a particular moment: KiCad 10.0.5, `kipy` 0.7.1, macOS 26.5.2 arm64, one board, one host font stack. Re-running the instrument on another machine — or on this one after a KiCad upgrade — is **not** expected to reproduce the digests, and nothing in the artifacts detects that. The exposure is that a later reader treats these numbers the way this repository's offline corpus numbers may be treated. They may not be: the offline censuses are reproducible from committed bytes by a committed runner, and these are not. Three things bound it. The instrument **refuses loudly and publishes no artifact** without a live session, so a machine that cannot reproduce the measurement produces nothing rather than a misleading `skipped` record. Every claim built on this evidence is scoped in its own row to one session, one board and one build, and both `Claim` lines say so. And [D-231](decision-ledger.md) is explicitly the decision about whether such evidence may ever bind a conversion — with the recommendation being that it may not, precisely because of this risk. The residual is a documentation risk rather than a product one: **no shipped code path consumes anything measured here**, and ADR-0095's refusal is unchanged, so a stale live number cannot reach a router, a DRC run, an apply, or a board. It is recorded because the *next* lane is the one that would be tempted to build on it. |
| R-183 | A closed census runner and its synthetic fixtures can be over-read in two directions before the external cohort exists: the fixtures can be mistaken for measured public-board evidence, and a head appearing in KiCad's parser vocabulary can be mistaken for a safe Board IR acceptance decision. The latter is especially sharp for `property`, `filled_polygon`, `hatch`, and `name`, which the current adapter names in an allowlist without closing every payload shape; copying that allowlist as precedent could silently discard nested semantics. A same-count cohort substitution could also make an apparently plausible result bind different boards. | Medium | High | [D-232](decision-ledger.md) makes absence executable rather than editorial: the new selection commitment is `None`, and both direct measurement and CLI publication refuse until it is assigned from the exact B-129 cohort. Selection must reproduce B-129's fingerprint and classifier, B-137's full ten-board successor partition, and B-136's independent two-entry commitment, so a same-count membership swap fails. Root and fill vocabularies are frozen from the KiCad 9/10 parser union, but outputs are shape-only aggregate observations and explicitly confer no acceptance. Polygon-bearing payloads stay opaque, parsing uses the repository's byte/depth/node limits, sources are re-read unchanged, and outputs exclude per-board identities, paths, source digests, geometry, atom values, and board bytes; only the aggregate cohort fingerprint and artifact self-identities remain. | Open until the exact cohort is restored and an independently reviewed, self-digested aggregate artifact receives its own `B-` row. Even then, field acceptance requires a separate source-grounded direction-of-error decision and production tests; D-232, ADR-0123, issue #231 |
| R-184 | Requests on equal-pitch but different lattice phases can produce physically intersecting copper whose absolute unit-edge and vertex keys are disjoint. Widening negotiated admission without naming that gap could make zero structural overflow look like a physical-legality proof; the same under-count can also cause repeated retries or a bounded refusal and therefore provides no route-quality guarantee. Multi-pin demand can be under-estimated if only the candidate endpoint identities are inspected, and a custom router could try to attach forged fill provenance to an otherwise valid candidate. | High | Medium | [ADR-0126](../adr/0126-negotiated-routing-admits-bounded-multi-pin-nets-on-request-local-lattices.md) keeps structural accounting explicitly heuristic and makes the existing exact whole-set physical-clearance pass mandatory before atomic publication. Tests construct a shifted-phase crossing with disjoint resource sets and require physical refusal. Demand scans all 2–32 selected-layer pads, uses ceiling division over the full bounding box and is capped; a fixture makes an intermediate pad define both extrema. Candidate and connection pad counts/endpoints are bound to trusted request state, the no-fill seam requires `fill_binding is None`, semantic replay compares it, and cancellation/work ceilings remain independent. | Mitigated for safety; open for completeness and quality until the successor corpus census and a held-out repair-improvement run land. Shifted-phase conflicts may still retry or refuse; no DRC, electrical or fabrication claim. D-233, SEC-169, issue #90 |
| R-185 | A whole-set clearance violation names two nets but not the branch that caused it. Treating that attribution as a multi-pin repair capability could replace the wrong path, delete or reorder untouched paths, disconnect or cycle the tree, rewrite historical search accounting, accept a forged or stale selection, spend unmetered pair/projection/topology/contact work, lose consumed work on refusal, or publish a locally legal route that still conflicts with the rest of the allocation. | Medium | High | [ADR-0127](../adr/0127-multi-pin-local-repair-replaces-one-proven-responsible-branch.md) requires exact bounded path-pair reproduction, domain-separated candidate/path/responsibility bindings protected by private capabilities, cumulative conflict and untouched-target projection, exact preservation of every unselected path and non-derived field, two canonical candidate-ID checks, inherited accounting checks, one exact shared topology-edge ledger, charged cancellation-aware contact predicates, preserved projection work on refusal/cancellation, acyclic/contact-preserving Board IR and all-pad validation, original and reconstructed final-work preflights, charged responsibility work, cancellation propagation and the ordinary whole-set physical recheck before atomic publication. The 27-mutant committed spec independently attacks each load-bearing gate. | Mitigated for safety; open for completeness and quality. Deterministic first-responsible selection, one attempt and conservative projection can decline a feasible repair; a dense tree may exhaust the unchanged private validator ceiling, and a longer replacement that no longer fits is refused before validation and final recheck. B-140 contains no repair run; #90 stays open until held-out deterministic improvement is measured. D-235, SEC-171, issue #90 |
| R-186 | A positive completion differential from one fixed corpus can be over-read as routing quality, generalisation, or physical-board evidence. The control and treatment share the exact B-140 population and B-088 reference-routed net set, but B-088's 70 routes are independent per-net routes and the negotiated arms are a mutually compatible whole-board allocation. The four envelope-refused boards never reach repair, one treatment board publishes one repaired result, and aggregate wire length and physical-check deltas are accounting rather than quality scores. Repeated local success can also hide a corpus-specific responsible branch, conservative refusals, or a future runner/artifact/configuration drift. | Medium | High | [B-141](benchmark-ledger.md) binds the exact 20-board manifest, B-140 admission partition, B-088 reference, source commit, runner bytes, configuration, self-digested report and independent commitment; control `repair_settings: null` is uninstrumented, treatment uses the fixed default bounded profile, both run twice, and the closed report exposes no board, net, candidate, path or geometry payload. The semantic guards require each arm's `outcome_breakdown["envelope_construction"]` to equal the fixed population's `boards_unable_to_form_a_two_request_envelope` count (**4**); forbid a disabled control (`repair_settings: null`) from claiming `completed_with_repair`, `repair_published` or any non-zero repair-work field, and require all six status categories (`completed`, `no_path`, `partial`, `invalid_request`, `cancelled`, `not_run`) to reconcile to the outcome taxonomy; the commitment pins exact control and treatment arm totals plus the full differential, and its whole-metrics digest `sha256:f7e38d6744feed63b852e10811f34205bb822a1e2e7ca9759a8cea80a326d4b2` covers every report metric. Per-reason refusal/outcome reconciliation is independently checked, and a later merge-ref or checkout movement of HEAD does not replace the historical source binding. The source guard verifies the actual Git runner blob SHA at the declared source commit and accepts later historical `HEAD` movement without replacing that binding; the closed `total_ripups` bound is `70 * (8 - 1) = 490`, with 490 accepted and 491 mutation-killed. The closed aggregate wire bound is `70 * 62,500,000,000 = 4,375,000,000,000 nm`, with the exact bound accepted and the `+1` boundary mutation-killed. The 187-test suite and 78/78 evidence-contract mutation harness have zero survivors or control failures and cover schema closure, source/runner/configuration/commitment bindings, population and arm parity, deterministic replay, accounting conservation and non-echoing output; #238's 35/35 capability mutants remain a separate result. [ADR-0127](../adr/0127-multi-pin-local-repair-replaces-one-proven-responsible-branch.md) keeps final whole-set physical clearance and atomic refusal as authority. Amended 2026-09-01 after twelve P2 review findings on the self-digest validator, which were one finding in twelve places: totals were authenticated but not their decomposition, their denominators, or their provenance. Every published breakdown now declares which run-outcome codes each bucket stands for; the declaration is checked for total, disjoint coverage and each bucket is reconciled against the codes it partitions, in the report and in the companion, so an item moved between buckets with the totals patched up is refused and a code that lands in no bucket fails the run rather than publishing a quiet zero. The `total_overflow_units` ceiling is now `70 * 250,000 = 17,500,000`, derived from the submitted nets and the grid each may occupy, replacing `admitted * iterations * submitted**2` = 627,200 -- a product of two population-wide aggregates and a per-board repeat count that was also small enough to refuse a truthful measurement from one dense board. `date_utc` is derived from the committer date of the recorded revision and checked against Git on authoritative load, rather than pinned as a literal. The commitment's arm key set is derived from the report's own arm schema, so `total_physical_checks`, `total_wire_length_nm`, every breakdown and every repair-work counter are pinned by name and a claimed aggregate added later without a pin fails the run. The republication reproduced the retired artifact's whole-metrics digest `sha256:f7e38d6744feed63b852e10811f34205bb822a1e2e7ca9759a8cea80a326d4b2` exactly, so the measurements were re-bound, not re-measured. `validate_report` returns the guarantee it actually reached -- `shape_only`, `offline` or `repository_bound` -- and its docstring enumerates what each level does and does not establish, because three of the twelve findings (the evidence date, the recorded source revision, and the companion's measurement pins) are bound to the repository rather than to the document and are therefore outside that function's scope at any argument. `_validate_authoritative_bindings` returns `repository_bound` or `companion_bound` depending on whether the companion was consulted, and `load_artifact` -- the authoritative entry point -- checks that it reached `LOAD_ARTIFACT_GUARANTEE` rather than asserting so in prose. Three re-signed documents (a lying evidence date, a source commit naming nothing, a moved headline total) are tested as accepted at the offline level and refused once the repository is consulted, pinning the content of the weaker guarantee in both directions. An absent recorded commit and a disagreeing one are separated: a repository that does not contain the recorded revision -- reachable only from an unrelated branch, a shallow checkout, a fresh clone, or another fork's artifact -- receives a refusal naming the absence, never the tampering refusal, because no disagreement was observed. That case refuses by default, and downgrades to the `offline` level only when a caller passes `allow_absent_source_commit`; `load_artifact` cannot opt in. | Mitigated for evidence integrity; open for held-out quality and human calibration. The result closes B-141's machine-executable evidence contract only. It does not close #90, and it does not establish KiCad DRC, electrical, SI/PI/EMC, thermal, DFM, fabrication, apply, editor, hardware or general-corpus validity. Human benchmark review/calibration remains required; the next agent-only direction is #91's private surrogate ranking with authoritative signoff. D-236, SEC-173, issue #90, issue #91 |
| R-187 | A private surrogate ranking can be mistaken for a physics, fabrication, or routing-quality authority. Candidates that differ in net, ordered endpoints, layer/width/pad count, ordering policy, fill binding, router version, policy or any A* setting could be incorrectly compared because the legacy candidate ID does not bind all comparison context. | Medium | High | ADR-0128 adds a separately recomputed comparison digest over the complete comparison context and refuses mismatches as `incomparable_candidates`; seed, geometry, cost and metrics remain the intended ranking differences. The result is closed, redacted and advisory-only, with bounded immutable inputs, deterministic scoring, no MCP/CLI/apply path, no production caller, DFM-only authoritative KiCad DRC unchanged, and SI/PI/thermal unregistered. Final 62-test suite and 23/23 mutation result have zero survivors/control failures. | Mitigated for private ranking; open for authoritative adapters, reviewed exposure, held-out quality, fabrication validation and human calibration. D-237, SEC-174, issue #91 |
| R-188 | **A live read taken across an unverified API binding can be mistaken for a verified one, and the older direction is the one that was already wrong.** ADR-0129 accepts editor/binding pairs that drift within a major version, which is strictly more acceptance than the surface offered before. The residual risk is a caller that treats `future_api_unverified` or `legacy_api_unverified` as interchangeable with `compatible` — the distinction is published, but nothing forces a caller to read it. The legacy direction carries the sharper edge: the binding may issue a command that did not exist in an older editor, and while that fails loudly at call time, an operator seeing a generic `ApiError` may not connect it to a version gap. | Low | Medium | The verdict is a required field on both live contracts and its accepted set is validated at construction on the observation, the editor context and the oracle, so a widened vocabulary cannot reach a caller unchecked. `VERIFIED_API_COMPATIBILITY` is a one-member set rather than an equality test, so widening what counts as proof is a visible edit that a mutant targets. **Live apply gates on that set and refuses both acceptances**, which keeps the risk confined to reads, whose worst case is an incomplete answer carrying the verdict that says so. Both version strings are published alongside the verdict, so drift direction and magnitude stay recoverable. The refusal across a major boundary names both versions and the reason. Mutation spec [`2026-09-01-live-version-binding.json`](../mutants/2026-09-01-live-version-binding.json) covers the classifier boundaries in both directions, the apply gate and the binding-disagreement guard. **A second, dated trigger:** the binding-agreement check pins `kipy` 0.7.1's comparison semantics, so a future `kicad-python` that relaxes `check_version()` would have its `True` treated as a contradiction and refused as `inconsistent` — this ADR reproducing the defect it fixes, one layer down. Recorded rather than pre-emptively softened, because the check is load-bearing today; a `kipy` upgrade is the trigger to revisit. | **Narrowed — owner `@seunghyukchoe`.** [B-142](benchmark-ledger.md) discharges the accept-path half against a real KiCad 10.0.5: both surfaces that refused B-138 observe, the verdict is `future_api_unverified` across a genuine `FutureVersionError`, and the binding-agreement guard does not misfire on a healthy session — the one guard a fake cannot exercise honestly, since its second read is the same constant as its first. **What remains open is narrower and named:** the `legacy_api_unverified` direction and the major-boundary refusal have never met a real editor, because that needs KiCad builds this host does not have, so both stay fake-only. The `kipy`-upgrade trigger above is untouched by B-142. |
| R-190 | **The source distribution ships untracked files, and the test that guards its allowlist cannot report that.** `tests/test_sdist_allowlist.py::test_no_tracked_top_level_entry_falls_out_of_the_sdist_unnoticed` populates itself from `git ls-files`, which is the right population for the direction it guards (does the allowlist still cover everything the repository tracks) and the wrong one for the direction that actually bit at 0.7.0. Hatchling honours `.gitignore` but not Git tracking, so an untracked, non-ignored file under an allowlisted directory is shipped. Verified rather than reasoned: an untracked `docs/UNTRACKED-SDIST-PROBE.md` was planted, `python -m build --sdist` was run, and the file appears in `copper_mcp-0.12.0.tar.gz` among its 924 entries. The exposure is a release artifact containing local scratch content that no review ever saw -- the same class as the 0.7.0 incident that swept twenty-eight agent worktrees into the sdist, bounded now by the allowlist to the seven allowlisted directories rather than the whole tree. | Medium | Medium | Partial before this merge. The allowlist itself is the bound: only `/src`, `/tests`, `/docs`, `/schemas`, `/benchmarks`, `/hardware` and `/scripts` plus named top-level files can carry anything, and `.gitignore` plus `.git/info/exclude` already keep `.venv/` and `.claude/worktrees/` -- the 0.7.0 vector -- out. `scripts/check_secrets.py` sweeps `--cached --others --exclude-standard`, so an untracked file carrying a credential fails the build before it could be packaged. Neither bounded ordinary untracked notes until this merge: `scripts/check_sdist_tracked.py` ([D-246](decision-ledger.md)), landing in the same commit, builds the source distribution and refuses when any member is absent from `git ls-files` apart from the backend-generated `PKG-INFO`, and it runs in `make lint` and in hosted CI on every change. | Mitigated / release. [D-239](decision-ledger.md) deliberately did not fix this in the link checker; the pre-build refusal it named is `scripts/check_sdist_tracked.py` ([D-246](decision-ledger.md)), landed in the same merge. Residual, carried by R-192: the gate costs a full sdist build on every run and needs the `build` package, its generated-file exception is a list of one that a second generated member would force someone to extend, and a release built where the gate did not run still ships whatever the working tree held. issue #244, issue #256 |
| R-192 | The C1 infra-health fixes trade one residual for another in three places. The sdist gate now costs a real build: it runs `python -m build --sdist` (about 1.8 s on this repository) and needs the `build` package, so `make lint` and every hosted leg carry that, and a machine without `build` cannot run it at all. Its generated-file exception is a list of one, so a backend that starts emitting a second generated member makes the gate refuse until someone extends the list -- the safe direction, but a refusal a release engineer has to diagnose before believing. `derandomize` is now scoped to hosted CI, which splits the residual rather than removing it: the gate everyone must pass cannot surface a counterexample outside its one fixed stream, and the local runs that still can are not a gate. The deterministic refusal examples bound only the two paths they name, under either profile. The oversized-child test now accepts two kill reasons, which is weaker than one; its strength is the size and reaped-process assertions a completing child would still break. The B-141 fresh-clone refusal is closed on main by #252/D-241 (rebind to the default-branch squash commit plus a hosted ancestry test); this record's release note adds no new mechanism beyond pointing at that one. | Low | Medium | Each new guard carries its own regression test. The sdist gate's include a miniature hatchling project that is really built and whose two untracked members -- one of them hidden only in `$GIT_DIR/info/exclude`, the case the modelled gate could not report -- must both be named, a fully tracked project that must produce no findings, and `main()`'s refusal branch, which no test reached before (a mutant emptying the finding list survived the whole file). Four committed mutants attack the refusal, the generated-file exception, the member-prefix stripping and the tracked comparison; all four are killed. The gate is wired into `make lint` so it runs on every change rather than at release time only. | Open / infrastructure; D-246, issue #250, issue #253, issue #255, issue #256 |
| R-193 | A remote policy provider could receive proprietary design features, influence routing beyond the intended advisory boundary, redirect credentials, consume unbounded quota, or be mistaken for route/DRC authority. | High | High | The direct-import-only adapter sends aliases and bounded scalar features only; omits raw identities, revision, bounds, geometry, Board IR, and apply data; allowlists the HTTPS host, rejects redirects, caps request/response/argument bytes, JSON depth/values, completion tokens and timeout, performs no retries, requires one exact tool call, maps aliases back to coordinator-owned immutable options, and returns a fixed non-echoing refusal. It is not in the MCP/CLI surface or closed negotiated-policy registry, and deterministic routing, candidate validation, KiCad DRC, and explicit apply remain downstream. | Open / external integration; D-247, ADR-0130, SEC-175 |
