# Security Review Ledger

| Review ID | Date | Scope | Evidence | Findings / disposition | Reviewer |
|---|---|---|---|---|---|
| SEC-001 | 2026-08-03 | Initial filesystem, MCP, secrets, and AI trust boundaries | Path and symlink escape tests; bounded-read tests; secret scan; threat model | Read-only MVP accepted. Remote exposure and mutation remain blocked pending dedicated reviews. | Initial maintainer |
| SEC-002 | 2026-08-03 | Python dependency audit | `pip-audit` reported `PYSEC-2026-1845` in pytest 8.4.2; the post-upgrade audit found no known vulnerabilities | Raised the development dependency floor to pytest 9.0.3; finding remediated and verified. | Initial maintainer |
| SEC-003 | 2026-08-03 | KiCad CLI DRC boundary | Injection; process/discovery timeout; in-process report, cumulative-byte, file-count, and non-overlapping snapshot-memory bounds; malformed/incomplete/schema-skew reports; stale-context; side-effect containment; redaction; and real KiCad 10.0.5 integration tests | Read-only fixed-argument adapter accepted; a POSIX wrapper sets the file ceiling before KiCad starts and execution occurs on a path-preserving private snapshot so `.kicad_prl` never enters the source workspace. Unsupported platforms fail closed. Arbitrary arguments, save/refill, and raw finding disclosure remain prohibited. | Initial maintainer |
| SEC-004 | 2026-08-03 | GitHub Actions supply chain | Reviewed upstream major-version changes, pinned each action to a full commit SHA, replaced the deprecated provenance wrapper with `actions/attest`, retained least-privilege job permissions, and passed current-main CI, security, CodeQL, labeler, and dependency-review checks | Workflows accepted. Future minor/patch updates are grouped while majors remain isolated for review. Release verification may run manually, while attestation and publication remain restricted to version-tag pushes. | Initial maintainer |
| SEC-005 | 2026-08-03 | `v0.1.0` release provenance and recovery | Tag run `30762649321`; artifact SHA-256 digests; GitHub attestation verification constrained to the repository, release workflow, tag ref, and source digest; clean-wheel installation | Build and attestation succeeded. Release creation failed because the isolated publish job lacked repository context; the identical verified artifacts were recovered manually, and the workflow was corrected to pass the repository explicitly without broadening permissions. | Initial maintainer |
| SEC-006 | 2026-08-03 | CodeQL v4 action-suite migration | Dependabot PRs `#14`, `#15`, and `#16`; mixed-generation failure logs; immutable upstream v4.37.4 commit `f205ea1c3313d32999d8d6a48b4f6530d4437b38`; full repository checks | The three coupled CodeQL actions were upgraded atomically. Dependabot now groups the suite across major, minor, and patch updates, preventing mixed action generations from being proposed independently. | Initial maintainer |
| SEC-007 | 2026-08-03 | Untrusted KiCad and Board IR v0.1 parser boundary | Exact integer/string numeric regressions under changed decimal context; streaming S-expression and pre-DOM JSON allocation regressions; default writer/decoder budget parity; property/construct-name redaction; quote-aware numeric nets; version/layer/setup/tenting/native-identity, positional-lock, copper/`Edge.Cuts` graphics, zone-semantics, outline, and via-span mutations; real CopperTone conversion | Accepted for the documented `20260206` subset after independent adversarial review. Unmodeled routing and non-default fabrication semantics fail without a snapshot. Remaining limits are intentional: one hole-free Board IR outline (rectangular in the KiCad adapter), front orthogonal footprints, full-stack through vias, solid single-loop zones, caller-supplied constraints, no fill-cache authority, no mutation/apply, and operational budgets may reject oversized schema-valid third-party input. | Initial maintainer + independent agent audit |
| SEC-008 | 2026-08-03 | Candidate-only A* routing boundary | Exact and one-nanometre edge/keepout tests; stale/forged/type-invalid inputs; unmodeled geometry and constraint rejection; grid, expansion, obstacle-count, obstacle-check, and cancellation limits; 250 fixed-seed randomized determinism replays; independent 200-case Dijkstra cost comparison; full 144-test gate | Accepted for the documented synthetic rectangular first slice after an independent review found and verified remediation of multiplicative obstacle work. Successful metrics are internal only. KiCad export, authoritative candidate DRC, MCP exposure, preview, and apply remain prohibited/unimplemented. | Initial maintainer + independent agent audit |
| SEC-009 | 2026-08-03 | Candidate-bound authoritative KiCad DRC boundary | Exact candidate/base/source/patched-board/context hash tests; fixed-argument private execution; patched per-file and cumulative limits; stale live and private board/rule/library membership rejection; malformed, oversized, timed-out, signalled, unexpected-exit, and exit/report-mismatch cases; aggregate/type-count consistency; valid warning, exclusion, and hard-failure evidence; deep evidence immutability; and real KiCad 10.0.5 inode/mtime/workspace preservation | Accepted for internal use on the documented replayed synthetic route. One subprocess/report path is shared with ordinary DRC; exit codes must agree with report finding presence; caller-controlled/save/refill flags and child logs remain blocked; and no candidate bytes or raw findings are returned. MCP/CLI exposure, preview, persistence, apply, electrical/SI/PI/EMC/thermal/DFM conclusions, fabrication approval, and production safety remain outside this evidence. | Initial maintainer + independent agent audits |
| SEC-010 | 2026-08-03 | Public route-preview boundary | Unknown-field, non-integer, boolean-as-integer, out-of-range, control-character, oversized-name, non-copper-layer, and non-object request rejection; workspace traversal rejection; fail-closed Board IR conversion with bounded diagnostic-code counts; wall-clock deadline cancellation; typed routing diagnostics; detached and immutable serialization; binding invariants between candidate, Board IR digest, board revision, and DRC evidence; stubbed and real KiCad 10.0.5 runs asserting unchanged workspace inode, mtime, and bytes | Accepted as a read-only public surface for the documented Board IR and two-pin subset. Candidate-bound DRC evidence is reachable only through the explicit `include_drc` flag, still returns aggregate counts only, and fails the call when KiCad is unavailable or evidence does not bind. Generated candidate geometry and endpoint pad IDs are intentionally returned to the caller; source board bytes, other nets, raw findings, coordinates, UUIDs, and net names are not. Durable jobs, persistence, export, and apply remain unimplemented. | Initial maintainer |
| SEC-011 | 2026-08-03 | Board IR inspection and shared request boundary | Unknown-field, missing-field, non-object, non-integer, boolean-as-integer, control-character, and invalid net-class request rejection; workspace traversal rejection; fail-closed conversion with diagnostic-code-only reporting; supported/unsupported state invariants; detached immutable serialization; explicit assertion that net names, pad/net identities, UUIDs, and coordinates are absent from the returned document; constraint-dependent digest binding | Accepted as a read-only structural surface. Disclosure is limited to object counts, digests, units, and standard KiCad copper layer names. Every public service now parses untrusted requests through one shared boundary, so a rule tightened there applies everywhere. Returning the Board IR snapshot itself, net names, or geometry remains out of scope. | Initial maintainer |
| SEC-012 | 2026-08-03 | Existing-copper obstacle model | Exact and quarter-turn pad extents; orthogonal segment sweeps with ceiling half-widths; stricter-of-two net-class clearance selection; obstacle-budget accounting across keepouts, pads, and segments; fail-closed rejection of arcs, zones, vias, off-axis rotations, diagonal segments, and partially routed nets; A*/Dijkstra cost agreement on pad, segment, and mixed-obstacle boards; and a real KiCad 10.0.5 run confirming a detoured route reports zero errors and zero unconnected items | Accepted. Obstacle work remains bounded by the existing obstacle-count and obstacle-check ceilings, and round pad shapes over-approximate only, so the model can refuse a legal route but cannot permit a clearance violation. Multi-net routing, vias, rip-up, and apply remain unimplemented. | Initial maintainer |
| SEC-013 | 2026-08-03 | Conservative polygon zone-boundary obstacle model | Concave-not-bounding-box, full-grid-edge crossing, exact/one-nanometre offset, diagonal 3:4:5 rational distance, strictest-of-three clearance, same-net/other-layer, object/vertex/edge budget, 64-check cancellation, deterministic replay, A*/Dijkstra agreement, and outline-only KiCad adapter-to-preview workspace-preservation tests; CopperTone 0/14 failure-funnel re-measurement; independent code audit; full 271-test, Ruff, strict mypy, secret scan, dependency audit, and package build gate | Accepted after the audit found that foreign-net class fallback swallowed `CANCELLED`; the fix rethrows every non-missing-class failure and regression tests cover both lookup checkpoints. Eager big-integer distance checks and a second unmetered vertex scan during polygon sorting were also removed. The complete outline is a deliberate over-approximation, cached fill remains non-authoritative, same-net zones remain partial routing, and no zone-specific KiCad refill/DRC, real-board routing, electrical, DFM, or fabrication claim is accepted. | Initial maintainer + independent agent audit |
| SEC-014 | 2026-08-03 | Licence-aware offline audio capability corpus | Exact licence-hash and SPDX-identity mismatch tests; reference-only redistribution gating; duplicate/path/symlink/URL/oversize rejection; disjoint and evidence-derived claim tests; required candidate IDs and observed typed refusals; catalog and artifact replacement-race tests; deterministic private-copy execution; source byte/mtime preservation; real KiCad 10.0.5 Gerber parse/plot smoke; 18 focused tests; 288 nonblocked repository tests; Ruff, strict mypy, secret scan, and independent remediation audit | Accepted for a development-only local functional corpus after the audit's licence-binding, fabricated-claim, TOCTOU, and provenance-wording blockers were remediated. Catalog, artifact, and licence report bindings come from one bounded validation snapshot; external records are metadata-only and never executed. This enforces repository provenance policy but is not legal, electrical, DRC, DFM, fabrication, or hardware evidence. The existing real-KiCad findings test still aborts in the managed macOS environment, while current dependency audit and isolated build attempts are network-blocked; release gating remains required in CI or an unrestricted environment. | Initial maintainer + independent agent audit |
| SEC-015 | 2026-08-03 | Canonical Circuit Intent IR `0.1.0` and deterministic KiCad schematic renderer | Strict schema/codec, duplicate/unknown/numeric/control/UTF-8/digest/topology rejection, tighten-only byte/tree/object ceilings, canonical replay, escaped-string injection tests, immutable input and fixture inode/mtime checks, content-verified source/count provenance, artifact tamper/drift rejection, UUID uniqueness at the 64-component/128-connection ceiling, 47 focused tests, real KiCad 10.0.5 SVG and exact `kicadxml` connectivity export, 335 nonblocked repository tests, Ruff, strict mypy, secret scan, and independent remediation audit | Accepted for the bounded two-pin resistor/non-polarized-capacitor logical subset after the audit found and verified fixes for caller-weakenable input/tree limits and independently drifting artifact provenance fields. Rendering is pure, capped at 1 MB, uses original embedded symbols, leaves footprints empty, sets `on_board=no`, and has no MCP/CLI/file-writing surface. Successful parsing and netlist export do not establish ERC, value correctness, symbol/footprint qualification, schematic-to-board parity, electrical/audio performance, manufacturability, or fabrication safety. The pre-existing real PCB DRC test still aborts in the managed macOS environment; dependency audit and isolated build remain network-blocked, so release gating is still required in CI or an unrestricted environment. | Initial maintainer + independent agent audit |
| SEC-016 | 2026-08-03 | Bounded Circuit Intent build and schematic delivery | Strict snapshot and structured-content validation; content-owned digest creation; byte-identical double-render requirement; redacted serialization checks; independent 256-bit token, TTL, count, aggregate/per-artifact byte, LRU, uniform-unavailable, and digest-tamper cases; stdio-only MCP tool/resource and absent HTTP registration; explicit workspace-confined create-new CLI export with traversal, symlink, suffix, existing-target, and partial-file rejection | Accepted for the ADR-0015 MVP boundary, superseding SEC-015's no-public-surface statement without weakening its renderer controls. Ordinary MCP output omits circuit strings and bytes; the capability store is non-enumerable, process-local, non-persistent, and limited to 15 minutes, 16 entries, 16 MiB total, and 1 MiB per artifact. CLI export creates one new schematic only. Per-build topology/digest/provenance/replay checks pass, while KiCad parse, ERC, parity, and electrical validation remain `not_run` and board readiness is false. A reviewed KiCad 10.0.5 fixture retained exact nets and reduced ERC warnings from seven to four; the two isolated external-port labels and two missing private-library-configuration warnings remain, so ERC-clean is not claimed. | Initial maintainer + independent agent review |
| SEC-017 | 2026-08-03 | KiCad CLI global-configuration and child-environment isolation | Exact child-environment capture; inherited token and KiCad-variable exclusion; private HOME/KiCad/XDG/runtime/temp roots; state-tree symlink and special-file rejection; per-file, file-count, cumulative-byte, and scan-time boundary cases | Accepted as hardening for ordinary and candidate-bound DRC. The child environment is reconstructed from `PATH=os.defpath`, `LANG=C`, and `LC_ALL=C` plus private per-run roots; no caller entry or user-global KiCad configuration is inherited. Private state is inspected under the same bounded policy as captured design context. KiCad remains a trusted external executable, and isolation does not turn DRC into ERC, electrical, DFM, or fabrication evidence. | Initial maintainer + independent agent review |
| SEC-018 | 2026-08-03 | Post-MVP filesystem, artifact-retention, and KiCad dependency audit remediation | Absolute non-renewing capability-expiry and lazy-reclamation cases; descriptor-anchored read and path-component replacement cases; exact-lowercase output suffix and held-directory create-only checks; file-table environment/absolute/remote/plugin URI rejection; snapshot-confined relative dependency acceptance; private-child-cwd capture; retained redaction, digest, state-budget, and context-revision checks | Accepted as clarification and hardening of SEC-016/SEC-017. Fifteen minutes is an access-control deadline, not immediate deletion or secure erasure; stale entries are reclaimed on later store activity or process exit. Workspace bytes are never consumed through a validate-then-reopen path, and output remains exact-lowercase create-only. KiCad receives only snapshot-confined table dependencies and runs from private `TMPDIR`, so uncaptured host, remote, plugin, and working-directory state cannot silently enter DRC evidence. Circuit Scene IR and semantic/visual placement remain an unimplemented high-fidelity north star, never a direct AI mutation path. | Initial maintainer + independent agent audit remediation |
| SEC-019 | 2026-08-03 | Final unrestricted audit remediation: MCP schemas, KiCad evidence capture, artifact accounting, and release authorization | Closed wrapper/content/output schema cases with scalar/list/extra non-echo rejection; no-follow nonblocking regular-report cases; duplicate/non-finite/depth/value JSON cases; read-only full private-snapshot-tree side-effect checks; post-insertion artifact-alias mutation and exact size-accounting checks; changelog and release-authorization tag-gate checks; unrestricted Python 3.12.13 desktop run with `CODEX_SANDBOX` absent, 386/386 tests plus 26 subtests, KiCad 10.0.5, and five real PCB DRC nodes | Accepted after the unrestricted audit. MCP shape errors cannot expand or echo the private contract; DRC evidence rejects special/replaced reports, hostile JSON, and any full-tree snapshot side effect; store identity and quotas use detached content/digest/size. All five real PCB DRC paths passed independently and preserved source bytes, mtime, and inode where applicable. The managed root run still has one environment skip/exit `-6`, but this no longer means real PCB DRC is unvalidated. Release tagging additionally requires a dated version section and append-only exact-commit `Ready` authorization; no `0.2.0` authorization is recorded. | Initial maintainer + independent unrestricted audit |
| SEC-020 | 2026-08-04 | Board IR `0.2.0` footprint fidelity, Circuit Scene `0.2.0`, and revision-bound placement projection | Strict schema/codec and legacy-compatibility cases; total pad ownership, courtyard-count/vertex/intersection, canonicalization, metamorphic-transform, scene-detail, native-identity, locked-move, unsupported-transform/topology, source-revision, forged-snapshot, and caller-tightened-budget regressions; independent adversarial review and remediation verification; direct KiCad 10.0.5 DRC of the four-pose courtyard fixture with zero violations and zero unconnected items; Python 3.12.13 `make check` with 881 passed and one pre-existing skip, Ruff, strict mypy, secret scan, dependency audit, and wheel/sdist build | Accepted for revision-bound observation and non-mutating placement preview after the audit found and verified fixes for two blockers: a stale digest could label forged footprint content as the old snapshot, and the placement-view `ParseLimits` argument was ignored. The view now enforces caller-tightened content ceilings before canonical digest verification. The KiCad adapter remains restricted to front-side orthogonal footprints and matching unfilled `fp_rect` courtyard centerlines; `courtyard_overlap` remains `not_modelled`, and placement apply remains prohibited. | Initial maintainer + independent agent audit |
| SEC-022 | 2026-08-04 | Revision-bound Scene-to-route MCP edge and closed status contract | Actual `mcp.call_tool` scene→route chain; exact candidate equality against the hidden-name oracle; board-revision refusal before Board IR conversion; snapshot-revision refusal before route/fill/DRC/token work; strict UTF-8 request boundary; selector XOR and relational constraint tests; contradictory five-variant output rejection with Draft 2020-12 validation; nested closed-schema regression; repeated capability-minting calls; real KiCad 10.0.5 candidate-DRC and fresh-fill evidence; Python 3.12.13 `make check` with 913 passed and one pre-existing skip, 90% coverage, Ruff, strict mypy, secret scan, pip-audit, and wheel/sdist build | Accepted for the supported scene/route subset. The opaque scene reference is directly actionable only when both observed revisions are supplied; stale captures fail closed and raw net names remain compatibility-only. The MCP 2026-07-28 specification permits any valid JSON Schema 2020-12 output, so the status union is intentionally root-discriminated; input remains object-rooted. `preview_route` stays read-only for the workspace, while `idempotent_hint=false` truthfully reflects fresh stateless HMAC capability issuance when explicitly requested. The benchmark is an integration baseline, not whole-board, multilayer, placement, live-IPC, electrical, DRC-generalization, or fabrication evidence. | Initial maintainer + independent agent audits |
| SEC-023 | 2026-08-04 | Optional KiCad IPC observer boundary | Fake-client contract tests cover local endpoint normalization, TCP rejection, timeout/size ceilings, deterministic digest/count redaction, missing optional dependency, and future-version refusal/opt-in. The bundled plugin manifest and entrypoint expose no mutation API. | Accepted for the redacted read-only observer: endpoint/path/token/board content is omitted, all reads are bounded, future API versions fail closed by default, and the tool returns `read_only=true`. A live KiCad session was not probed because this workstation has KiCad IPC disabled; no live-scene, placement, routing, DRC, electrical, or fabrication claim is made. | Initial maintainer |
| SEC-024 | 2026-08-04 | KiCad IPC snapshot-to-Circuit-Scene hand-off | Exact source-byte/digest/size invariant; fake-client deterministic scene replays; native Board IR conversion; author-text quarantine; literal `board: "live"`; malformed and stale board/snapshot digest refusals; closed MCP input/output schema; live render refusal; full local release gate. | Accepted for read-only semantic binding: the source hand-off is internal and never serialized, only exact Board IR/Circuit Scene fields cross MCP, and caller-supplied stale revisions fail closed. The workstation's IPC server is disabled, so B-009 is a fake-client contract baseline; live placement, routing, DRC, ERC, electrical, and fabrication evidence remain absent. | Initial maintainer |
| SEC-025 | 2026-08-04 | KiCad IPC review remediation | False `check_version()` results now fail closed; object counts are parsed from one bounded serialization and a second serialization must match; ten mutable collection materializations were removed; malformed source and revision-change tests were added; benchmark redaction uses identifiable private-object markers; plugin discovery-directory installation is documented. | Accepted with one explicit residual: the official synchronous wrapper allocates the complete `get_as_string()` response before Python can enforce the byte ceiling. The adapter rejects oversized data immediately after return, bounds the parser, avoids additional object allocations, and records the need for an isolated worker before any hard pre-allocation or remote-session guarantee. No live session was available because IPC is disabled. | Initial maintainer + review-bot remediation |
| SEC-026 | 2026-08-04 | Live Circuit Scene request preflight | The live-scene application service validates the closed board/constraint/region/layer/digest shape before `capture_live_board`, rejects the unsupported render flag at the same boundary, and uses a generic non-echoing error. B-011 proves one malformed request caused zero fake IPC client calls. | Accepted: malformed MCP traffic cannot trigger board serialization, source parsing, or count materialization. Valid requests retain the exact source/digest and stale-revision gates from SEC-024. A live GUI session remains unavailable because IPC is disabled. | Initial maintainer + review-bot remediation |
| SEC-027 | 2026-08-04 | KiCad IPC serialized topology-count correctness | Recursive S-expression count traversal now marks only direct `kicad_pcb` children as net declarations, ignores nested `(net ...)` references for the net total, and includes KiCad `gr_circle` graphics in the bounded shape set. Regression tests and B-012 use one top-level net plus one nested pad reference and one circle. | Accepted: redacted object counts now describe board topology rather than child reference repetition, while parser budgets and the byte-identical confirmation remain unchanged. The fake-client result does not establish live KiCad compatibility or complete graphical coverage beyond the documented set. | Initial maintainer + review-bot remediation |

| SEC-028 | 2026-08-04 | Live KiCad IPC route-proposal boundary | Fake official-client exact-source tests; stale board/snapshot refusal; action flags rejected before IPC; closed MCP schema; deterministic file-backed candidate equality; redacted output assertions; benchmark B-013; source and full release gates | Accepted for the read-only proposal subset. The tool accepts only `net_ref_id` and both revision preconditions, converts one bounded byte-confirmed IPC snapshot, and returns no DRC/fill/apply authority or mutation. A real KiCad IPC session, live editor transaction, DRC, placement, electrical, and fabrication evidence remain unclaimed. | Initial maintainer |
| SEC-029 | 2026-08-04 | Live KiCad IPC placement-proposal boundary | Fake official-client exact-source tests; candidate equality with file-backed placement; stale board/snapshot refusal; unknown action field rejected before IPC; closed MCP request/response schema; deterministic B-014 replay; mutator guard; Ruff, strict mypy, and targeted MCP/placement tests | Accepted for the read-only proposal subset. The tool requires `board: "live"` and both Scene revisions, derives all poses through the existing Board IR/legalizer path, and returns no raw source, DRC, fill, apply authority, or editor mutation. The workstation IPC server is disabled, so no live GUI, KiCad DRC, undo, electrical, or fabrication claim is made. | Initial maintainer + review-bot remediation |

Security reviews are required before adding remote authentication, provider integrations, uploaded
files, persistent multi-user jobs, or candidate application.

> **Amendment — 2026-08-03 (append-only correction):** SEC-004's row was edited in place by
> PR #17 rather than superseded by a new dated entry, which the append-only convention forbids.
> The row as first written in PR #13 read: *"Reviewed upstream major-version changes, pinned each
> action to a full commit SHA, replaced the deprecated provenance wrapper with `actions/attest`,
> and retained least-privilege job permissions"*, with the outcome *"CI, security, release,
> labeler, CodeQL, and Scorecard workflows accepted pending current-main GitHub checks; future
> minor/patch updates are grouped while majors remain isolated for review."* PR #17 replaced both
> fields with the current text after the dry-run work landed. The current row is factually right
> and is left standing; this note restores the history the in-place edit removed. Git history is
> the authority for both versions.
